# PSD：实时模拟交易账户与交易能力优化计划

## 文档信息

- 文档类型：PSD（产品与系统设计文档）
- 日期：2026-09-23
- 状态：待实施
- 适用产品：K 线训练营 2.0
- 目标范围：训练模拟交易与实时筛选模拟交易的账户隔离、完整行情输入、账本完整性和统一交易接口

## 一、背景与目标

当前产品已经具备训练模拟交易和基于实时行情的模拟交易，但两者尚未形成稳定、清晰的共同交易边界：

- 训练使用完整 K 线和完整执行引擎；实时模拟成交刷新只传递部分 K 线字段。
- 训练与实时模拟共用一组账户设置，无法分别选择收益率模式或资金账户模式。
- 实时模拟账本以品种为中心保存资金，多个品种之间没有真正共享账户资金。
- 实时模拟账本只保存简化字段，无法完整承载止损、止盈、风险仓位、费用、部分成交、保证金和引擎版本等信息。
- 页面入口虽然部分共用下单函数，但实时模拟仍有独立的成交、持仓、现金和盈亏计算路径。

本计划的最终目标是：

1. 训练和实时模拟交易拥有彼此独立的账户配置与运行状态。
2. 训练账户和实时模拟账户都能独立选择收益率模式或资金账户模式。
3. 实时模拟账户在资金账户模式下按市场独立：A 股与美股各自拥有账户，同一市场内的多个品种共享现金、权益、预留资金和风险预算。
4. 实时模拟成交可以取得与训练相同结构的完整已收盘 OHLCV K 线。
5. 订单、持仓、成交、拒单和账户快照能够完整、可版本化地持久化。
6. 所有下单入口通过同一个交易命令契约进入共享交易核心；训练和实时只负责提供不同的时钟、行情和存储适配。
7. 训练与实时模拟共同使用设置页中的同一套“确定性成交引擎”参数，不再保留实时模拟的隐藏零成本执行配置。
8. 保持模块化单体，不引入微服务、消息总线、全局状态库或第二套交易规则。

## 二、已确认的产品决策

### 2.1 账户边界

- 训练模拟账户与实时模拟账户完全独立。
- 训练中的订单、持仓、现金和盈亏不得影响实时模拟账户。
- 实时模拟账户中的订单、持仓、现金和盈亏不得影响任何训练会话。
- 训练账户仍以单次训练会话为运行边界；每个训练在创建时冻结账户模式和初始资金，恢复历史训练时继续使用快照中的账户配置。
- 实时模拟账户以市场为资金边界并跨页面刷新持久存在；A 股与美股互不占用资金，同一市场内切换观察品种不会创建新的资金副本。

### 2.2 两类账户都支持两种模式

训练账户和实时模拟账户分别拥有自己的：

- 账户模式：收益率模式或资金账户模式。
- 初始资金或风险计算基准。
- 当前现金、权益和预留资金状态。
- 同一套确定性成交引擎配置的运行快照。

收益率模式的语义与训练保持一致：

- 不因购买力不足拒绝普通固定数量订单。
- 持仓与成交仍保留价格、数量和方向。
- 实时表现沿用训练的收益率计算和展示口径，不另外维护一套实时收益率算法。
- 多品种汇总继续使用训练已有的方向与名义金额处理规则，避免按品种数量简单平均造成偏差。
- 风险百分比仓位需要使用独立的风险计算基准资金；该基准只用于计算下单数量，不构成购买力约束。

资金账户模式的语义：

- A 股和美股分别创建独立的实时模拟资金账户，不跨市场混用资金。
- 同一市场内所有实时模拟品种共享该市场账户的现金和权益。
- 同一市场内任一品种的挂单都会占用该市场账户的预留现金或保证金。
- 同一市场内任一品种成交、费用、平仓盈亏都会更新该市场账户。
- 风险百分比仓位按照当前品种所属市场账户的权益和可用资金计算。
- 同一市场内的多品种订单必须在同一账户快照和同一版本上完成校验，避免超额使用资金。
- 不直接合计人民币与美元金额；跨市场总览以独立市场卡片展示。

### 2.3 设置与运行状态的区别

- 设置页保存的是“新训练默认账户配置”和“新实时模拟账户默认配置”。
- 已创建训练的实际账户配置继续保存在训练快照中，修改默认设置不能改变进行中的训练。
- 已创建实时模拟账户的实际模式、初始资金和当前余额保存在实时账户账本中，不能只依赖普通偏好设置。
- 任一市场的实时模拟账户已经存在持仓、挂单或成交时，修改默认模式不得静默改写该市场账户；用户需要结束并新建对应市场账户，或在该市场账户完全为空时修改。

### 2.4 确定性成交引擎设置

- 设置页只保留截图所示的一套“确定性成交引擎”设置，不为训练和实时分别增加第二套参数。
- 佣金、最低佣金、滑点、买卖价差、单根 K 线成交量参与上限，以及同一根 K 线同时触发止损与止盈时的处理规则，训练与实时模拟全部按这套设置执行。
- 新训练创建时冻结当时的设置快照，恢复训练时继续使用该快照。
- A 股或美股实时模拟账户首次创建或切换到统一执行引擎时，也冻结当时的设置快照；后续设置修改不追溯改写已发生的订单和成交。
- “共用设置”指配置来源和计算语义相同，不代表训练账户与实时账户共享资金、订单、持仓或运行状态。

## 三、本计划范围与明确排除项

### 3.1 本计划包含

- 训练账户设置与实时模拟账户设置拆分。
- 实时模拟账户按市场独立，并在市场内跨品种共享资金。
- 实时行情成交输入扩展为完整已收盘 OHLCV。
- 实时账本字段补全、版本化和兼容读取。
- 统一订单、持仓、成交和账户领域契约。
- 统一下单命令入口和环境适配边界。
- 训练功能在实时模拟中的能力映射与可用性展示。
- 训练和实时共同接入同一套确定性成交引擎设置。
- 数据迁移、并发控制、测试和安装包首次使用闭环。

### 3.2 本计划暂不包含

- 不重新计算、重放或改写已有成交历史。
- 不推导日线内部最高价、最低价、止损和止盈发生的先后顺序；同 Bar 冲突继续使用既有确定性规则并保留冲突标记。
- 不在本阶段把实时筛选从现有主要日线能力扩展到分钟线、逐笔或盘中连续撮合。
- 不在本计划中启用新的佣金、滑点、价差或部分成交默认值。
- 不接入真实券商，不发送真实订单。
- 不把实时筛选市场扩展到外汇或黄金；本阶段实时筛选和实时模拟账户仍以 A 股、美股为范围。
- 不改变训练快照格式之外的训练执行规则，除非是为账户设置拆分所需的兼容字段。

本计划会处理新版统一执行的版本边界和切换步骤，但仍坚持“历史事实不重算”：已有成交、已平仓持仓和历史资金结果保持原样，只对明确切换后的新事件应用新版语义。

## 四、问题一：训练与实时模拟账户设置混在一起

### 4.1 当前问题

当前应用设置只有一组账户模式和初始资金。训练创建和实时观察都从这组设置取值，造成：

- 用户无法让训练使用收益率模式、同时让实时模拟使用资金账户模式。
- 修改训练默认值可能间接影响之后创建的实时模拟记录。
- 实时账户的长期状态与训练会话的短期状态没有清晰区分。
- 设置页中的“模拟交易账户”无法说明设置究竟影响训练还是实时模拟。

### 4.2 目标行为

设置页将“模拟交易账户”拆成两个独立设置组：

1. 训练模拟账户
   - 账户模式：收益率模式或资金账户模式。
   - 资金账户模式下配置“新训练初始资金”。
   - 收益率模式下保留风险仓位所需的风险计算基准。
   - 只影响之后新建的训练；已开始或已保存训练不变。

2. 实时模拟账户
   - 账户模式：收益率模式或资金账户模式。
   - 收益率模式沿用训练的计算与展示语义。
   - 在同一个实时账户设置组中，分别配置“A 股初始资金”和“美股初始资金”，或分别配置收益率模式使用的风险计算基准。
   - 账户模式是实时模拟默认配置；A 股和美股在首次使用时各自创建独立账户实例并冻结模式和本市场初始资金。
   - 当前市场账户为空时可以直接应用；存在持仓、挂单或成交时，引导用户新建对应市场账户，不能静默重置。
   - 明确说明它是模拟交易，不会向券商发送订单。

### 4.3 设置数据兼容

- 保留读取旧的账户模式和初始资金字段。
- 首次读取旧设置时，将旧值迁移为“训练模拟账户”的默认值，确保训练行为不变。
- “实时模拟账户”的默认值在迁移时复制旧值，但迁移完成后与训练设置独立保存。
- 新设置写入后继续保留必要的旧字段兼容别名，直到所有入口都完成迁移；不得让旧版本客户端写回时删除新字段。
- 设置归一化必须分别校验两个账户配置，损坏其中一个配置时只回退该配置，不能连带重置另一个配置。

### 4.4 运行状态接入

- 新建训练时只读取训练账户默认配置，并冻结到训练状态和快照。
- 打开实时筛选结果时只读取该品种所属市场的实时模拟账户，不再复制训练页面当前的现金、模式或初始资金。
- 在同一市场内切换品种时，只切换品种账本视图，账户余额保持为该市场共享余额。
- 从 A 股切换到美股时，切换为另一个市场账户，不能沿用 A 股现金或权益。
- 退出实时观察、刷新页面或重启应用后，A 股和美股实时模拟账户分别从实时账本恢复。

### 4.5 主要影响位置

- `web/app/features/settings/settingsContracts.ts`
- `web/app/features/settings/components/SettingsPanel.tsx`
- `web/app/features/settings/settingsGateway.ts`
- `web/app/components/TrainingWorkbench.tsx`
- 训练快照构建、恢复和设置兼容测试
- 实时状态加载、保存和首次创建流程

### 4.6 验收标准

- 训练和实时模拟可以分别保存不同账户模式。
- 修改训练账户设置不改变当前或历史实时模拟账户。
- 修改实时账户默认设置不改变进行中的训练。
- 新训练准确采用训练默认账户配置。
- 新建 A 股和美股实时模拟账户时，准确采用实时默认模式及各自市场的初始资金。
- 已有训练快照可以正常恢复。
- 旧偏好数据升级后训练行为保持原状。
- 设置页明确显示两个账户的用途、当前状态和下一步操作。

## 五、问题二：实时模拟多标的资金被拆成多份

### 5.1 当前问题

实时账本以品种为单位保存账户模式、初始资金和现金余额。用户在同一个市场观察多个标的时，每个标的可能拥有独立现金副本，无法形成市场内真实的跨品种资金约束。同时，A 股与美股币种和交易规则不同，也不能被错误合并为一个资金池。

### 5.2 目标账户模型

新增账户级状态，账户负责：

- 账户标识和显示名称。
- 市场身份和账户币种；本阶段分别为 A 股账户和美股账户。
- 账户模式。
- 初始资金或风险计算基准。
- 当前现金。
- 已占用资金、已预留资金和可用资金。
- 所有持仓按最新价计算后的账户权益。
- 账户版本号、创建时间、更新时间和状态。

品种账本只负责：

- 品种身份和市场。
- 该品种的订单、持仓、成交、拒单和最后处理行情时间。
- 该品种的实时观察上下文、筛选来源和决策记录。

每个品种账本引用其所属市场的实时模拟账户，不再各自拥有独立现金余额。A 股品种只引用 A 股账户，美股品种只引用美股账户。

### 5.3 两种账户模式下的跨品种规则

资金账户模式：

- 购买力在市场账户层统一计算，A 股和美股互不影响。
- 创建订单时，对当前市场账户的现金、保证金和全部挂单预留进行一次统一校验。
- 成交后在同一事务内写入成交、持仓、订单状态和账户余额。
- 同一市场内多个品种同时成交时按确定顺序处理，并使用账户版本防止重复占用资金。
- 全部平仓提供“当前品种全平”和“当前市场账户全平”两个明确范围；不提供默认跨市场全平。

收益率模式：

- 计算规则和展示口径与训练收益率模式一致。
- 不建立购买力限制，但订单、持仓和成交仍归属对应市场账户。
- 市场内总收益使用训练已有的统一口径，不把每个品种误当成一份独立本金。
- 风险百分比仓位使用账户的风险计算基准，不读取训练账户资金。

### 5.4 既有多品种账本迁移

由于现有每个品种都可能有独立现金余额，不能把这些余额直接相加，也不能任意选取其中一个余额作为共享账户余额。

安全迁移方式：

- 某个市场没有任何实时订单、持仓或成交：直接为该市场创建新的共享实时模拟账户。
- 已经存在实时记录的用户：保留现有品种账本并继续正常展示，不再让它承担新共享账户的资金来源。
- 为之后的新订单按市场创建共享实时模拟账户，模式取实时账户默认设置，初始资金取该市场的实时默认资金。
- 既有品种账本不重新计算、不合并现金、不删除；页面按普通实时模拟标的呈现，关联状态只用于内部判断能否交易。
- 页面可以同时展示既有品种记录与当前 A 股、美股模拟账户，但不能把独立账本金额错误并入共享账户或跨币种合计。

这项迁移解决多标的资金重复问题，同时避免用不可靠的方式拼接已有资金状态。

### 5.5 并发和一致性

- 每个市场账户的余额更新必须在服务端数据边界内完成原子事务。
- 每个账户保存递增版本号；更新必须携带读取时版本，版本过期则重新读取并重试，不能覆盖新状态。
- 订单 ID、成交 ID和账户命令 ID必须具备幂等性；重复刷新或重复提交不能产生第二笔成交。
- 多浏览器窗口同时刷新时，同一个市场账户只有一个窗口能够成功应用同一个行情批次。
- 账户状态、订单状态、持仓状态和成交记录必须同成同败，不能只写入其中一部分。
- 页面不得直接拼装数据库状态，只能消费实时交易应用服务返回的账户快照。

### 5.6 主要影响位置

- `web/db/schema.ts`
- `web/db/runtime.ts`
- `web/db/live-ledger.ts`
- `web/app/api/live-state/route.ts`
- `web/app/features/live/liveGateway.ts`
- 新的实时账户应用服务或控制器
- `web/app/lib/tradingAccount.ts` 中账户汇总和收益率口径
- `web/app/components/TrainingWorkbench.tsx` 中实时账户读取和页面组合逻辑
- 实时表现汇总控制器、市场筛选和账户卡片

### 5.7 验收标准

- A 股和美股各有一份独立现金和权益，不跨市场混合。
- 同一市场的实时资金账户只有一份现金和权益，不随品种数量复制。
- 在一个 A 股品种挂单后，另一个 A 股品种的可用资金同步减少，美股账户不受影响。
- 同一市场两个品种同时成交时不会超额使用同一份资金。
- 训练下单、平仓或恢复不会改变实时账户。
- 收益率模式的计算和展示结果与训练收益率模式一致，且不因现金不足拒绝普通订单。
- 资金账户模式严格执行市场内共享购买力。
- 页面刷新、应用重启和多窗口刷新后账户金额一致。
- 既有品种账本仍可正常查看，且不被错误合并进新账户。

### 5.8 实时表现页面的模式化展示

实时表现不能继续用同一套摘要同时解释收益率模式和资金账户模式。页面先按实时账户模式选择展示结构，再按市场筛选账户数据。

收益率模式：

- 展示逻辑与训练表现中的收益率模式保持一致。
- 主要指标包括总收益率、浮动收益率、已实现收益率、交易胜率、持仓数量、待成交数量和已平仓数量。
- 可以提供全部市场、A 股、美股筛选；全部市场使用统一收益率口径，不显示跨币种资金合计。
- 不展示现金、可用资金、持仓市值等资金账户专属指标。

资金账户模式：

- 默认先选择市场账户，再展示该市场的资金数据。
- A 股账户以人民币展示，美股账户以美元展示；不得直接把两个市场金额相加。
- 账户主卡展示初始资金、当前现金、持仓市值、账户权益、可用资金和已预留资金。
- 收益卡展示已实现盈亏、浮动盈亏、总盈亏和账户收益率。
- 交易统计展示持仓、待成交、已平仓、胜率和拒单数量。
- “全部市场”视图只并列展示 A 股账户卡和美股账户卡，不提供虚假的合计现金、合计权益或合计盈亏金额。
- 当前市场没有账户时显示建立账户入口；账户读取失败时显示影响范围和重试入口。

展示数据必须直接来自标准化市场账户快照和其关联的品种账本，不得再从每个品种的独立 `cashBalance` 临时相加。

实时表现页验收：

- 收益率模式与训练表现使用相同的格式、正负号、分母和空数据规则。
- 资金账户模式切换 A 股和美股时，币种、现金、权益和盈亏随市场账户正确切换。
- 同一市场任一品种成交后，该市场账户卡与品种明细同步更新。
- 跨市场总览不合计人民币和美元金额。
- 既有品种账本正常展示；未关联市场账户的金额不混入当前账户指标。

## 六、问题三：实时成交输入缺少完整 OHLCV

### 6.1 当前问题

训练 K 线已经拥有完整的开、高、低、收、成交量和成交额；实时刷新链路却只把历史候选 K 线压缩为时间、开盘和收盘。底层数据具备能力，但接口和适配器丢失了字段。

此外，当前历史候选最多读取固定数量的早期 K 线。如果一个委托长期未触发，持续从创建时间读取同一批早期 K 线，可能永远无法处理更晚的行情。

### 6.2 目标数据契约

实时成交使用标准化的完整已收盘 K 线：

- 时间戳。
- 开盘、最高、最低、收盘。
- 成交量，可为空但不能伪造。
- 成交额，可为空。
- 行情是否已收盘。
- 数据源和必要的质量标记。

该契约应与执行引擎的 K 线输入一致，不再定义只有开盘和收盘的第二种成交 K 线。

### 6.3 A 股数据链路

- TDX 和 BaoStock 历史日线在底层读取时保留完整 OHLCV。
- 实时行情可以继续提供当前价格、当日开盘、成交量和成交额用于展示。
- 未收盘的当日实时行情不得作为完整日线执行保护性退出或盘中触价。
- 收盘确认后，将完整日线放入待处理行情序列。
- 本地行情服务、Web API 和 live gateway 全程保留高、低、成交量和成交额，不再在中间映射时裁剪。

### 6.4 美股数据链路

- 从现有 candles 数据表读取完整 OHLCV，而不是只选择时间、开盘和收盘。
- 继续使用纽约市场收盘判断，未结束的当日 K 线只用于展示，不进入确定性成交处理。
- 数据更新完成后，实时账户读取新增的已收盘 K 线并按时间顺序处理。

### 6.5 增量游标和分页

- 每个实时账户品种保存“最后已处理 K 线时间”，而不是每次从最早订单创建时间重新扫描。
- 接口按最后处理时间返回之后的已收盘 K 线，并提供是否还有下一页。
- 一次刷新存在大量积压时分批处理，但必须保持时间升序。
- 每个批次处理完成并成功持久化后才推进游标。
- 处理中断时从最后已确认游标恢复，不能跳过，也不能重复成交。
- 行情必须进行排序、去重和 OHLC 合法性校验；不合法 K 线进入可见错误，不得静默参与成交。

### 6.6 主要影响位置

- `web/local-data/legacy-tdx-store.mjs`
- `web/local-data/baostock-store.mjs`
- `web/local-data/server.mjs`
- `web/app/api/live-scan/route.ts`
- `web/app/features/live/liveGateway.ts`
- `web/app/lib/liveOrderExecution.ts`
- 实时账户品种游标和行情批次处理服务

### 6.7 验收标准

- A 股和美股的实时成交输入包含完整 OHLCV。
- 未收盘 K 线不会触发成交、止损或止盈。
- 盘中触及限价或突破价、但开盘未触及的已收盘 K 线可以被完整识别。
- 成交量缺失时按明确的“不限量”兼容规则处理，不把缺失量当成零成交量。
- 超过一个批次的积压行情可以连续处理到最新位置。
- 网络中断或页面刷新不会丢失或重复处理 K 线。
- 训练数据读取和图表显示不因实时接口扩展而改变。

## 七、问题四：实时模拟账本字段不完整

### 7.1 当前问题

当前实时账本只保存简化的订单、持仓和成交字段。条件委托、止损止盈等字段临时借用价格区间 JSON 保存；持仓和成交中的费用、风险、部分成交、保证金和执行版本等信息无法完整往返。

这会导致：

- 页面刷新后交易对象丢字段。
- 新功能必须继续借用无关字段存储。
- 无法证明某笔成交使用了哪一版规则和配置。
- 实时表现、复盘和问题诊断缺少必要证据。

### 7.2 目标账本结构

账户、订单、持仓、成交和拒单使用共享领域定义，不再在 `TrainingWorkbench` 内复制一套页面私有类型。

订单至少持久化：

- 身份、账户、品种、动作、方向和状态。
- 委托类型、触发价、创建时间、计划执行时间。
- 原始数量、已成交数量、剩余数量。
- 止损、止盈。
- 固定数量或风险百分比仓位模式。
- 风险比例、风险预算。
- 预留现金、预留保证金。
- 市场规则版本、执行引擎版本和完整载荷版本。

持仓至少持久化：

- 入场订单、方向、数量、平均成本和入场时间。
- 入场委托类型和是否在 K 线内触发。
- 止损、止盈和初始风险。
- 已实现盈亏、毛盈亏、费用。
- 平仓信息、出场原因和同 Bar 冲突标记。
- 风险仓位信息、保证金和品种经济参数。

成交至少持久化：

- 原始行情价、报价侧价格和最终成交价。
- 买卖报价侧、手续费和价格影响成本。
- 委托类型、触发价和成交原因。
- 部分成交标记、剩余数量和名义价值。
- 保证金影响、账户币种和执行引擎版本。

### 7.3 存储策略

采用“稳定查询列 + 版本化完整载荷”的混合方案：

- ID、账户 ID、品种 ID、状态、时间、数量和关键价格使用正式字段，便于查询和约束。
- 费用明细、经济参数、纪律信息和后续扩展字段保存在版本化完整载荷中。
- 每条记录保存载荷版本，读取时按版本归一化。
- 不再将订单元数据塞入价格区间字段。
- 写入阶段同时校验正式字段和完整载荷的一致性。
- 读取旧记录时补兼容默认值，但不把推断值回写成历史事实。

### 7.4 数据库迁移要求

- 使用追加式迁移，不删除现有表或字段。
- 迁移前读取现有记录并验证主键、品种归属和父子关系。
- 旧数据保留原值；无法可靠推导的字段保持缺失并显示为未知。
- 迁移过程可重复运行，重复启动不会复制记录。
- 任一步失败时保留旧账本可读，不留下半迁移账户。
- 安装包升级路径必须自动执行迁移，不依赖用户手工命令。

### 7.5 服务端边界

- UI 只发送交易命令或读取账户快照，不直接提交整套可覆盖账本的任意对象。
- 服务端应用层负责加载账户、执行命令、校验版本并持久化差量。
- 数据库访问继续经过 API、service 和 runtime 边界，live feature 不直接调用数据库运行时。
- 读取接口返回标准化账户快照，隐藏表结构和兼容字段。

### 7.6 主要影响位置

- `web/app/lib/executionEngine.ts` 的共享领域类型出口
- 新的共享交易契约模块
- `web/db/schema.ts`
- `web/db/runtime.ts`
- `web/db/live-ledger.ts`
- `web/app/api/live-state/route.ts`
- `web/app/features/live/liveGateway.ts`
- `web/app/components/TrainingWorkbench.tsx` 中的页面私有交易类型和序列化兼容逻辑

### 7.7 验收标准

- 新订单、持仓和成交保存后再读取不丢任何领域字段。
- 旧账本可以读取并保持已有展示结果。
- 价格区间字段只保存价格区间，不再承担订单元数据容器。
- 重复迁移不会生成重复订单、持仓或成交。
- 损坏的单条记录被隔离并产生可恢复提示，不导致整个账户无法打开。
- 页面和服务端使用同一套稳定交易契约。

## 八、问题五：下单入口共用表面流程，但执行仍然分叉

### 8.1 当前问题

训练和实时模拟虽然部分共用页面状态和委托创建函数，但实时行情刷新仍然手工计算成交、持仓、现金和盈亏。结果是同一功能需要在多个分支分别修改，容易出现只改训练、漏改实时的情况。

### 8.2 统一交易命令边界

所有入口统一生成标准交易命令，至少覆盖：

- 提交开仓委托。
- 提交平仓委托。
- 撤销委托。
- 修改保护价格。
- 当前品种全平。
- 整个账户全平。

交易命令必须携带明确的：

- 账户和运行环境身份。
- 品种、市场和时间周期。
- 动作、方向、数量或风险仓位参数。
- 委托类型、触发价、止损和止盈。
- 用户看到的行情版本或账户版本。
- 幂等命令 ID。

页面不再直接修改持仓、成交或现金，只消费统一命令返回的接受、拒绝和最新账户快照。

### 8.3 共享交易核心与环境适配

共享交易核心负责：

- 数量和市场规则校验。
- 风险仓位计算。
- 委托生命周期。
- 成交、部分成交和拒单。
- 止损止盈和持仓变化。
- 现金、费用、保证金和权益变化。
- 确定性事件和版本信息。

环境适配器只负责：

- 训练适配器：回放游标、历史 K 线、训练结束边界和训练快照。
- 实时适配器：已收盘行情批次、持续账户、行情处理游标和实时账本。

训练特有的快进、隐藏未来、训练时段和训练结束不进入实时功能。实时特有的行情刷新、筛选来源和账户长期保存不进入训练功能。

### 8.4 功能同步原则

以下交易功能只保留一个权威实现，并在两个环境中按能力开放：

- 固定数量和风险百分比仓位。
- 市价、限价和突破委托。
- 委托触发价。
- 止损、止盈和保护价格移动。
- 跳空不买。
- 买入开仓、允许市场的卖出开仓、单仓平仓和全平。
- 市场交易单位、禁止做空、T+1 和价格限制。
- 订单、待成交、持仓、成交历史和拒单展示。

同一市场规则不支持某项功能时，界面应显示为不可用并解释原因，而不是维护另一套实现。

### 8.5 UI 复用边界

- 提取有明确输入、输出和行为测试的共享下单面板，不提取只转发 props 的薄包装层。
- 共享控件负责账户模式、仓位方式、委托类型、触发价、止损、止盈、数量和操作按钮。
- 页面 shell 提供当前账户快照、市场能力、行情状态和命令回调。
- 训练与实时可以有不同的说明文字和状态提示，但字段含义、校验和命令结构一致。
- 正常、加载、空白、失败、账户不可用和保存冲突状态均提供明确恢复动作。

### 8.6 与既有实时记录的关系

统一契约、完整账本、账户边界和行情输入完成后，训练与实时入口改为调用同一个交易命令服务。切换遵循第九章的问题一实施计划：

- 不重放已有成交。
- 不自动转换已有待成交委托。
- 不静默改变既有实时记录的执行语义。
- 账户、订单、持仓和成交均保留执行版本，最终移除旧实时成交路径前必须通过独立切换验收门槛。

### 8.7 主要影响位置

- `web/app/lib/executionEngine.ts`
- `web/app/lib/liveOrderExecution.ts`
- 新的共享交易命令和账户应用服务
- `web/app/features/training`
- `web/app/features/live`
- `web/app/components/TrainingWorkbench.tsx`
- 下单面板及订单/持仓展示组件
- 执行引擎、风险仓位、市场规则和实时订单测试

### 8.8 验收标准

- 训练和实时下单按钮进入同一个交易命令入口。
- 页面组件不再手工创建成交或直接计算账户现金。
- 新增一个共享交易字段时，两个入口不需要分别定义和保存。
- 相同账户状态、相同市场规则和相同完整 K 线输入产生同结构的确定性状态转换。
- 训练专属流程和实时专属流程保持隔离。
- CodeGraph 不出现 live feature 反向读取训练组件内部状态的路径。

## 九、此前讨论遗留风险的实施计划

本章沿用此前讨论的问题编号。按本次决定，不处理问题二“日线内价格先后顺序”和问题三“实时筛选周期范围”；以下规划问题一、四、五、六、七、八。问题六不新增第二套设置，而是让训练和实时共同使用现有“确定性成交引擎”设置。

### 9.1 问题一：统一执行切换策略

#### 9.1.1 推荐结论

采用“账户级版本切换、历史事实冻结、旧委托不转换、持仓只结转事实”的策略。不要在某次升级后让全部旧实时数据直接进入新版执行引擎，也不要长期让新旧引擎同时处理同一张订单。

切换边界定义为：

- 已成交记录、已平仓持仓、历史现金变化和历史表现只读保留，不重算费用、不改成交价、不补做过去本应发生的成交。
- 旧版待成交委托不自动翻译为新版委托；启用新版前必须撤销旧委托，避免同一委托在两套触发规则下得到不同结果。
- 旧版未平仓持仓可作为“结转持仓”带入新账户版本，只保留确定事实：方向、数量、平均成本、入场时间、已实现盈亏和已收费用。
- 旧版止损、止盈和条件元数据只有在能够完整、无歧义读取时才允许转成新版保护委托；否则保留为历史说明，并要求用户在新版账户中重新确认保护价格。
- 切换后的新下单、撤单、修改保护价格和平仓全部进入统一交易命令与共享执行核心，不再回到旧实时成交分支。

#### 9.1.2 版本与边界字段

市场账户增加明确的执行边界信息：

- 当前执行引擎版本。
- 切换状态：旧版、待切换、新版、切换失败待恢复。
- 切换时间和切换前账户版本。
- 切换时采用的市场规则版本和行情数据契约版本。
- 旧委托数量、结转持仓数量和需要用户处理的异常数量。

订单、持仓、成交、拒单和账户资金事件分别保存自身使用的执行版本，不能只依赖账户当前版本反推历史。

#### 9.1.3 切换流程

1. 先上线完整账本、账户版本、共享交易命令和只读兼容能力，但保持旧账户继续按旧路径运行。
2. 对每个 A 股或美股市场账户独立执行预检，核对账户余额、持仓数量、待成交委托、保护价格和账本父子关系。
3. 账户存在旧版待成交委托时进入“待切换”状态，停止创建新的旧版委托，并要求先撤销旧委托；不得自动转换或静默撤销。
4. 将未平仓持仓转成带来源版本的结转持仓，核对总数量、平均成本和已实现盈亏，不重新撮合入场。
5. 在一个事务中写入切换快照、执行版本、费用快照、结转结果和新账户版本；全部成功后才把账户标记为新版。
6. 新版账户首次刷新只处理切换游标之后的已收盘 K 线，绝不能再次处理切换前行情。
7. A 股和美股分别切换；一个市场失败不得阻塞或污染另一个市场账户。

#### 9.1.4 回滚与故障恢复

- 切换前生成不可变备份快照，但不复制出第二套可同时交易的账户。
- 事务提交前失败时保持旧账户可用；提交后不允许简单降回旧引擎，避免新版事件被旧引擎再次处理。
- 提交后发现问题时，先暂停该市场的新命令，保留已产生的新事件，再通过修复迁移恢复；不得删除新版成交后重新运行。
- 所有切换动作使用幂等命令 ID，应用重启或重复点击不会生成第二份账户、第二批结转持仓或重复资金事件。
- 旧执行路径只有在所有可迁移账户完成切换、兼容读取稳定且没有旧版活动委托后才可删除。

#### 9.1.5 主要影响位置

- 实时账户应用服务和账户状态机
- `web/db/live-ledger.ts` 及追加式迁移
- `web/app/lib/executionEngine.ts`
- `web/app/lib/liveOrderExecution.ts`
- `web/app/features/live` 的账户切换提示和命令入口
- `web/app/components/TrainingWorkbench.tsx` 中旧实时执行分支的逐步收口

#### 9.1.6 验收标准

- 旧成交、旧已平仓记录和旧表现数值在升级前后完全一致。
- 旧委托不会在新版引擎中被自动触发，也不会与新委托重复存在。
- 结转持仓的方向、数量、成本和既有盈亏守恒。
- 新版账户不会重新处理切换时间之前的 K 线。
- 任一切换步骤失败后，用户能够看到影响范围并继续恢复，不出现半旧半新的账户状态。

### 9.2 问题四：复权、拆股和分红等公司行为

#### 9.2.1 推荐结论

公司行为必须成为独立、可追溯、幂等的账户事件，不能通过修改历史成交价或直接覆盖持仓成本来“修正”。第一阶段优先接入现有能力能够可靠识别的现金分红；送股、拆股、合股等数量型事件只有在数据源提供标准化比例和唯一事件身份后才启用。

#### 9.2.2 价格口径不变量

- 每根执行 K 线保存价格口径：原始价、前复权、后复权或数据源未知。
- 每个公司行为事件保存来源、事件 ID、除权除息日、登记口径、币种、每股金额或数量调整比例。
- 同一经济影响只能计入一次：若执行价格序列已经包含某项调整，账户事件处理器不得再次把相同影响计入收益。
- 图表可继续使用适合分析的复权口径，但成交证据、账户记账和公司行为必须能证明各自使用的口径。
- 数据源无法说明复权口径时，暂停该事件的自动记账并显示“待确认”，不得猜测。

#### 9.2.3 现金分红

- 复用训练侧已有的现金分红计算能力和统一公司行为归一化模块，不在实时 feature 内再写一套规则。
- 根据除权除息日前已持有的合格数量生成唯一资金事件；同一账户、品种和公司行为 ID 只能应用一次。
- 资金账户模式将分红记入对应市场账户现金，同时写入收益构成和账本流水。
- 收益率模式按训练现有口径计入表现，不生成虚假的购买力变化，但仍保存分红事件供复盘。
- 空仓、在生效日之后买入或数据证据不足时不记账，并保存明确原因。

#### 9.2.4 数量型公司行为

- 对拆股、合股、送股等事件，按标准化比例同时调整持仓数量和单位成本，保证事件前后持仓经济价值连续。
- 对仍有效的限价、突破、止损和止盈价格按相同比例调整，并生成新的订单版本；不能只改持仓而遗漏保护委托。
- 市场最小交易单位、零碎股和舍入结果由市场规则模块决定；舍入差额必须形成可解释事件，不能静默丢失。
- 已平仓历史只展示公司行为，不改写历史数量和成交价。
- 数据源没有稳定的比例、事件 ID 或更正机制时，本类事件保持只读提示，不启用自动调整。

#### 9.2.5 处理顺序与幂等性

对同一交易日按固定顺序处理：先应用生效的公司行为，再校验和调整存量委托，最后处理该日可执行的已收盘 K 线。每次成功处理后同时推进公司行为游标和账户版本；任一步失败则整批回滚。

公司行为被数据源撤销或更正时，不直接删除已记账事件。系统写入更正事件并保留前后版本；若更正会改变已产生的账户资金，暂停该市场账户自动处理并要求显式确认或重建账户，避免静默改写历史。

#### 9.2.6 主要影响位置

- `web/app/lib/corporateActions.ts`
- 市场数据归一化与复权口径元数据
- 实时行情批次处理服务
- 账户资金事件、订单版本和持仓版本账本
- 公司行为标记、实时表现明细和错误恢复界面

#### 9.2.7 验收标准

- 同一现金分红重复刷新只记一次。
- 资金账户现金变化与分红流水一致，收益率模式与训练口径一致。
- 拆股后数量与成本按比例变化，持仓经济价值连续，关联保护委托同步调整。
- 已复权价格和显式公司行为不会造成双重收益。
- 来源不明或更正中的事件不会被自动猜测处理。

### 9.3 问题五：历史行情修正不得触发追溯成交

#### 9.3.1 推荐结论

实时模拟采用“处理时证据冻结”原则。行情源后来修改已经处理过的 K 线时，可以更新图表和数据质量状态，但不得自动回到过去新增、删除或改变成交。用户若希望按修正后的完整历史重算，应新建一个模拟账户或显式创建重建副本，而不是改写当前账户。

#### 9.3.2 行情版本与执行证据

- 每根 K 线形成稳定身份，由市场、品种、周期和时间戳确定。
- 每次数据内容变化形成新的行情修订版本，并保存来源更新时间与内容摘要。
- 交易批次保存实际使用的 K 线修订版本；产生成交、拒单、止损或止盈时，额外保存当时的完整 OHLCV 证据快照。
- 账户维护“最后已处理时间”和“最后已处理修订”两个维度，避免只靠时间戳无法识别数据被更正。
- 新出现且尚未处理的已收盘 K 线使用当前最新有效版本；已经处理过的时间点永不自动再次进入执行引擎。

#### 9.3.3 修正处理规则

- 只改成交量等非价格字段时，记录修正并更新分析数据，不重算成交。
- 修改开高低收且该 K 线已经参与执行时，标记受影响的订单、持仓或成交为“行情后来修正”，保留原执行证据和结果。
- 修正后的高低价本来会触发一笔过去未发生的委托时，不补成交；原高低价曾触发而修正后不再触发时，也不撤销原成交。
- 尚未处理的 K 线在进入执行前被修正，直接使用通过完整性校验的最新版本。
- 发现修正版本不合法或时间倒退时，暂停推进该品种游标并提供重试，不能跳过后继续处理。

#### 9.3.4 重建与对比

- 提供“基于最新历史数据新建模拟账户/重建副本”的显式能力，原账户自动归档或继续只读保留。
- 重建必须使用新的账户 ID、创建时间和执行版本，不与原成交 ID 共用。
- 可提供原结果与重建结果的差异摘要，但不能把重建结果覆盖原账户。
- 自动刷新、应用重启和安装包升级均不得触发隐式重建。

#### 9.3.5 主要影响位置

- 行情归一化与修订版本存储
- 实时账户品种游标
- 交易批次、成交证据和账本审计字段
- 实时表现中的数据修正标记
- 账户重建和归档流程

#### 9.3.6 验收标准

- 已处理 K 线被任意修正后，成交数量、成交价和账户资金保持不变。
- 用户能查看产生原结果时使用的 OHLCV 和行情修订版本。
- 未处理 K 线使用最新有效版本，且不会重复成交。
- 重建产生独立账户和独立结果，原账户完整保留。

### 9.4 问题六：训练和实时共同使用确定性成交引擎设置

#### 9.4.1 当前差异与目标

当前训练会在新训练创建时读取并冻结设置页中的确定性成交参数，而实时模拟仍可能使用隐藏的零成本、次日开盘默认配置。这会导致相同订单和相同 K 线在训练与实时得到不同的手续费、成交价、成交数量或同 Bar 退出结果。

目标是保留设置页现有的一套参数，并让训练和实时模拟统一读取、统一校验、统一计算：

- 佣金（bp）。
- 最低佣金。
- 滑点（bp）。
- 买卖价差（bp）。
- 单根 K 线成交量参与上限（%）。
- 同一根 K 线同时触发止损与止盈时的确定性处理规则。

不新增“训练费用设置”和“实时费用设置”，也不改变用户当前配置的默认数值。

#### 9.4.2 单一配置来源

- `AppSettings` 中的现有执行配置继续作为唯一权威设置来源。
- 设置保存、读取、归一化和损坏回退只保留一个入口，训练和实时不得分别实现默认值。
- 实时执行移除对隐藏零成本配置的直接依赖；所有新实时交易命令都从所属市场账户的配置快照取得参数。
- 页面只负责编辑设置，不能在下单时临时覆盖佣金、滑点、价差、成交量上限或冲突策略。

#### 9.4.3 运行时冻结规则

为保证结果可复现，共用设置采用“同一来源、分别冻结”的方式：

- 新训练创建时冻结当前配置，训练保存、恢复和继续时使用同一快照。
- A 股和美股实时模拟账户在各自创建时冻结当前配置；两个市场账户创建时间不同，允许暂时使用不同快照版本，但来源仍是同一个设置页面。
- 设置被修改后，只影响之后新建的训练和实时市场账户。
- 尚无订单、持仓和成交的空实时账户可以显式同步到最新设置；非空账户不得在运行中静默更换参数。
- 已有实时账户从旧零成本路径切换到统一执行时，在切换边界冻结当前配置；边界之前的成交保持原值，边界之后的新命令使用新快照。

#### 9.4.4 各参数的统一执行要求

- 佣金与最低佣金由共享费用计算器产生，训练和实时使用相同币种、数量和名义金额输入。
- 滑点与买卖价差进入共享成交价解析流程，页面和实时适配器不得再次叠加。
- 单根 K 线成交量参与上限为 0 时沿用当前整单成交语义；大于 0 时，训练和实时都按相同规则处理部分成交与剩余委托。
- 同 Bar 止损止盈冲突使用设置中选定的规则，实时不得固定写死为另一种优先级。
- 市价、限价、突破、止损、止盈和强制平仓都通过同一参数快照计算；任何绕过共享执行核心的路径视为未完成迁移。

#### 9.4.5 账本与可追溯性

- 训练快照和实时市场账户保存完整执行配置快照及其版本或内容摘要。
- 每笔订单和成交保存实际使用的执行配置版本、费用明细、价格影响和部分成交证据。
- 旧实时记录缺少配置证据时标记为旧版执行，不反推或伪造当时参数。
- 设置修改、账户创建和执行版本切换分别记录时间边界，以便解释同一用户不同阶段的结果差异。
- 历史成交不补收费用、不退回费用、不重新计算成交价，也不因设置变化重放。

#### 9.4.6 界面调整

- 保留“确定性成交引擎”单一设置卡，不新增重复字段。
- 将现有“实时观察仍沿用零成本、次日开盘模型”的说明改为：训练和实时模拟均使用该设置；参数在新训练或新实时市场账户创建时冻结。
- 设置修改成功后说明生效范围，区分“之后新建”与“当前运行中”的账户。
- 训练页面和实时账户详情可以只读显示当前使用的配置摘要，帮助用户确认实际口径，但不在交易页面提供临时修改入口。

#### 9.4.7 主要影响位置

- `web/app/features/settings/settingsContracts.ts`
- `web/app/features/settings/components/SettingsPanel.tsx`
- 训练创建、保存和恢复流程
- 实时市场账户创建与执行版本切换流程
- `web/app/lib/executionEngine.ts`
- `web/app/lib/liveOrderExecution.ts`
- 订单、成交及账户快照账本
- 确定性执行、费用和实时订单测试

#### 9.4.8 验收标准

- 设置页只有一套确定性成交引擎参数，没有训练和实时两份配置。
- 相同配置快照、相同账户状态和相同完整 K 线输入，在训练与实时产生相同结构的成交、费用和剩余订单结果。
- 实时模拟不再固定使用隐藏的零佣金、零滑点或另一套同 Bar 冲突规则。
- 设置修改不改变进行中的训练、非空实时账户或历史成交。
- 新建训练与新建实时市场账户采用当时最新的同一设置。
- 页面刷新、应用重启和训练恢复后，仍能确认实际采用的配置快照。

### 9.5 问题七：实时表现必须随账户模式切换

#### 9.5.1 推荐结论

在第 5.8 节的展示要求基础上，新增独立的实时表现汇总边界。表现层只消费标准化市场账户快照和共享统计结果，不再从页面状态、品种现金副本或 UI 组件中临时拼装指标。

#### 9.5.2 数据与计算边界

- 输入固定为：当前账户模式、市场账户快照、关联品种账本、最新估值价格和统计截止时间。
- 收益率模式直接调用训练已有的收益率、已实现收益率、浮动收益率、胜率和空数据处理逻辑，不复制公式。
- 资金账户模式由账户服务提供现金、持仓市值、权益、可用资金、预留资金和盈亏分解；页面只负责格式化和布局。
- 同一市场内所有品种先按市场账户汇总，再生成账户卡；禁止先生成每个品种的“账户资金”后相加。
- A 股与美股保持币种隔离。“全部市场”只并列返回两个市场摘要，不返回跨币种金额合计字段。
- 既有品种记录按普通标的展示；归档账户仍按账户状态管理。账户摘要只统计当前活动账户，未关联账本不并入账户资金指标。

#### 9.5.3 状态更新与一致性

- 下单接受、拒单、成交、撤单、平仓、公司行为、行情估值和账户切换成功后，都以服务端返回的新账户版本刷新表现。
- 表现摘要携带账户版本和统计截止时间；明细与摘要版本不一致时显示加载状态，不短暂展示拼接后的错误金额。
- 行情读取失败时保留上一次已确认摘要并标记估值时间，不能把缺失行情当成零价格。
- 旧数据字段不完整时显示“未知”或兼容标记，不把缺失费用、成交量或保证金推断成零。

#### 9.5.4 页面结构

- 收益率模式只显示训练口径的收益和交易统计，不混入现金类指标。
- 资金账户模式先显示市场选择，再显示该币种下的资金、权益、盈亏和交易统计。
- 模式来自账户快照而不是设置页当前默认值，避免用户改设置后当前账户页面突然换口径。
- 正常、加载、空账户、历史账户、数据过期、部分字段未知和读取失败状态都有明确提示及恢复操作。

#### 9.5.5 主要影响位置

- `web/app/lib/tradingAccount.ts` 的共享统计口径
- 新的实时表现汇总控制器
- 实时账户查询接口与标准快照
- `web/app/components/TrainingWorkbench.tsx` 的表现区域组合逻辑
- 实时账户卡、市场筛选和订单/持仓统计组件

#### 9.5.6 验收标准

- 相同收益率模式交易记录在训练表现和实时表现中得到相同结果与格式。
- 资金账户模式的账户卡可以由账本资金事件逐项复核。
- A 股和美股切换时币种、余额、权益和统计范围正确切换。
- 修改设置默认模式不会改变当前活动账户的表现页面。
- 任一品种成交后，市场账户摘要和该品种明细使用同一个账户版本。

### 9.6 问题八：实时模拟账户重置与生命周期

#### 9.6.1 推荐结论

账户“重置”不应删除或覆盖账本，而应结束当前市场账户并创建新账户。A 股和美股分别维护生命周期；每个市场同一时间最多一个活动账户，历史账户保持只读可查。

#### 9.6.2 账户状态

市场账户至少具有以下状态：

- 待创建：设置已准备，但该市场还没有账户。
- 活动：允许下单并接收行情批次。
- 待结束：用户已请求结束，但仍有待成交委托、持仓或正在处理的行情批次。
- 已归档：禁止新命令，只允许查看、导出和复盘。
- 异常暂停：迁移、行情或账本一致性异常，需要恢复后才能继续。

状态变化由账户应用服务执行，页面不能通过清空本地数组模拟重置。

#### 9.6.3 新建、结束与重置规则

- 首次进入某市场的实时模拟交易时，根据实时账户默认模式、该市场初始资金和当前确定性成交引擎设置创建账户并冻结配置快照。
- 用户请求重置时先预检待成交委托、持仓、未完成命令和数据异常。
- 账户完全为空时，可在一个事务内归档旧账户并创建新账户。
- 存在待成交委托时必须先撤销；存在持仓时必须先由用户平仓，或明确选择“保持旧账户归档且持仓只读”的受限流程。默认不强制成交、不虚构平仓价。
- 归档前冻结最后账户快照、表现摘要、执行版本和行情游标。
- 新账户获得新的账户 ID、初始资金和游标，不继承旧订单、持仓、成交或命令 ID。
- A 股账户重置不影响美股账户，反之亦然；训练账户完全不参与实时账户生命周期。

#### 9.6.4 并发与恢复

- 新建、归档和重置使用幂等命令 ID与账户版本校验，重复点击只产生一个新账户。
- 同一市场两个窗口同时重置时，只有一个事务能够成功，另一个窗口重新读取结果。
- 创建新账户失败时旧账户保持原状态；旧账户归档成功与新账户创建必须同成同败。
- 异常暂停账户提供重试、查看原因和保留历史的恢复入口，不允许通过删除本地数据绕过。
- 安装包升级后自动恢复活动账户和生命周期状态，不依赖手工数据库操作。

#### 9.6.5 页面与确认信息

- 设置页和实时表现页都显示当前市场账户状态、模式、创建时间和最近更新时间。
- “重置账户”操作改为“结束并新建账户”，确认界面列出会被归档的账户、待撤订单、未平持仓和新账户将采用的设置。
- 若当前无法结束，界面给出具体阻塞项及对应操作，不只显示通用失败。
- 历史账户按市场和时间查看，并明确标记只读，不能与当前活动账户合并统计。

#### 9.6.6 主要影响位置

- 实时账户 schema、状态机和事务服务
- `web/db/live-ledger.ts`
- 实时账户 API 与 gateway
- 设置页、实时表现页和订单/持仓面板
- 应用启动恢复与安装包升级冒烟

#### 9.6.7 验收标准

- 每个市场最多存在一个活动实时模拟账户。
- 重复重置、页面刷新或应用重启不会创建重复账户。
- 旧账户历史始终可查，重置不会删除成交或改写表现。
- 有待成交委托或持仓时不会静默清空账户。
- A 股、美股和训练的账户生命周期互不影响。

## 十、实施顺序与依赖

### 阶段 0：现状固化和特征测试

- 固化当前训练账户设置、实时账本读取、实时行情刷新和订单序列化行为。
- 为旧偏好、旧实时账本和重复 ID清理补充特征测试。
- 记录当前账户模式、现金、订单、持仓、成交和表现统计样本，作为迁移后只读兼容检查依据。
- 固化训练当前读取确定性成交设置、实时当前沿用隐藏默认配置的差异，作为统一后的回归基线。
- 不修改交易结果。

### 阶段 1：拆分账户设置

- 定义训练账户默认配置和实时账户默认配置。
- 完成设置归一化、旧字段迁移和设置页双账户界面。
- 确定性成交引擎设置继续只保留一套，不随账户设置拆分。
- 新建训练只消费训练配置。
- 实时账户创建只消费实时配置。
- 此阶段不改变实时账本资金结构。

### 阶段 2：建立按市场独立的共享实时模拟账户和完整账本

- 新增 A 股、美股市场账户级持久化和账户版本。
- 为订单、持仓和成交增加完整版本化载荷。
- 建立活动、待结束、已归档和异常暂停等账户生命周期状态。
- 完成旧账本只读迁移和按市场创建新共享账户。
- 将实时状态写入改为原子命令或受版本保护的事务。
- 页面改为读取标准账户快照。

### 阶段 3：扩展完整 OHLCV、行情修订和执行证据

- 扩展 A 股本地数据链路。
- 扩展美股数据库查询。
- 引入每品种最后处理时间、分页和完整性校验。
- 为 K 线增加稳定身份、修订版本和内容摘要。
- 让交易批次与成交保存实际使用的 OHLCV 证据和行情修订版本。
- 已处理 K 线被修正时只记录差异和影响标记，不追溯重算成交。
- 只把已收盘 K 线交给交易应用服务。

### 阶段 4：接入公司行为

- 明确各市场执行价格的复权口径，建立防止重复计入的校验。
- 先接入可由现有数据可靠识别的现金分红，并复用训练现有计算能力。
- 建立公司行为 ID、处理游标、资金事件和幂等约束。
- 仅在标准化比例与事件身份可靠时启用拆股、合股或送股调整。
- 公司行为更正进入可追溯更正流程，不静默覆盖已处理事件。

### 阶段 5：统一交易命令和页面入口

- 抽取共享领域契约和交易命令。
- 将现有确定性成交引擎设置作为训练和实时的唯一执行参数来源。
- 让训练快照和实时市场账户分别冻结来自同一来源的配置快照。
- 让训练下单入口通过统一命令服务。
- 让实时下单入口通过同一命令服务和实时账户适配器。
- 提取共享下单面板并根据市场能力展示可用状态。
- 保留对既有实时记录的兼容边界。

### 阶段 6：实时表现与账户生命周期闭环

- 建立只消费标准账户快照的实时表现汇总控制器。
- 收益率模式复用训练统计口径，资金账户模式按市场账户输出资金和权益。
- 完成账户首次创建、结束、归档、异常暂停和新建流程。
- 有委托或持仓时阻止静默重置，并提供明确处理入口。
- 完成多窗口幂等、重启恢复和历史账户只读查看。

### 阶段 7：执行版本切换

- 为账户和交易对象写入执行版本及切换边界。
- 逐市场预检旧委托、未平持仓、账户余额和行情游标。
- 旧待成交委托先撤销，未平仓持仓按事实结转，不重放历史。
- 事务化写入切换快照和当前确定性成交配置快照，并从切换游标之后启用统一执行。
- 完成故障注入、重复切换和回滚恢复验证后，才停止旧路径的新写入。

### 阶段 8：收口和交付验证

- 删除已无调用者的页面内手工成交和临时序列化逻辑。
- 保留必要的旧数据读取迁移器，禁止新写入继续产生旧格式。
- 完成训练/实时隔离、跨市场账户隔离、市场内跨品种共享、重启、多窗口和错误恢复冒烟。
- 完成实时表现页在收益率模式与资金账户模式下的模式化展示验收。
- 确认训练与实时已共同使用设置页中的确定性成交参数，实时路径不再使用隐藏零成本配置。
- 确认历史行情修正不会改变旧成交，公司行为不会被重复记账。
- 确认所有可迁移活动账户已完成版本切换后，再移除无调用者的旧实时执行路径。
- 更新架构文档、领域不变量、数据库迁移说明和安装包首次使用说明。

## 十一、测试与验证计划

### 11.1 设置测试

- 旧单账户设置迁移为训练默认和实时默认。
- 两个账户设置分别保存、读取和校验。
- 一个账户配置损坏时另一个保持可用。
- 修改默认设置不改变进行中的训练或已有实时账户。
- 资金账户初始资金和收益率模式风险基准分别生效。
- 拆分账户设置后，确定性成交引擎仍只有一套设置来源和一张设置卡。

### 11.2 账户测试

- 训练账户和实时账户状态完全隔离。
- A 股和美股市场账户状态完全隔离。
- 同一市场的实时资金账户跨两个品种共享现金和预留资金。
- 并发订单不会重复占用资金。
- 收益率模式与训练使用同一计算口径，且不执行购买力拒单。
- 账户模式在对应市场存在持仓、挂单或成交时不能被静默切换。
- 当前品种全平和当前市场账户全平具有不同且明确的作用范围。
- A 股挂单不会占用美股账户资金，美股挂单不会占用 A 股账户资金。

### 11.3 实时表现测试

- 收益率模式复用训练收益率计算、格式和空数据规则。
- 资金账户模式按市场展示初始资金、现金、持仓市值、权益、可用资金和预留资金。
- 已实现、浮动和总盈亏只汇总当前市场账户及其品种。
- 全部市场视图并列展示市场卡片，不合计不同币种金额。
- 市场切换、行情刷新、成交、撤单和平仓后账户卡与明细一致。
- 未关联市场账户的既有品种账本不混入当前市场账户汇总，但仍按普通实时模拟标的展示。
- 表现摘要与明细使用同一个账户版本；行情失败时不把缺失价格当零。
- 修改默认账户模式不会改变现有活动账户的展示口径。

### 11.4 行情输入与历史修正测试

- A 股和美股返回完整、排序、去重后的 OHLCV。
- 未收盘日线不进入成交处理。
- 无成交量数据时保持空值并按兼容规则执行。
- 超过单页限制的积压 K 线能够完整分页处理。
- 中断恢复不跳过也不重复处理行情。
- 非法 OHLC 被拒绝并显示可恢复错误。
- 已处理 K 线的开高低收或成交量被修正后，不新增、删除或修改历史成交。
- 每笔受影响交易可读取当时实际使用的 OHLCV 和修订版本。
- 未处理 K 线被修正后只使用最新有效版本。
- 基于修正行情重建时产生独立账户，不覆盖原账户。

### 11.5 公司行为测试

- 现金分红在同一账户和事件 ID 下只记一次。
- 资金账户现金流水和收益率模式表现分别符合各自口径。
- 执行价格已包含调整时，不重复计入同一公司行为。
- 数量型事件启用后，持仓数量、成本和关联保护委托同步调整且经济价值连续。
- 公司行为数据缺失、无唯一身份或被更正时停止自动猜测，并进入可恢复状态。

### 11.6 账本测试

- 订单、持仓、成交和拒单完整字段往返不丢失。
- 旧记录可以正常读取。
- 迁移可重复执行且不复制数据。
- 写入失败时账户、订单、持仓和成交全部回滚。
- 多窗口版本冲突不会覆盖较新的账户状态。
- 相同命令 ID重复提交只产生一次业务结果。

### 11.7 统一命令测试

- 固定数量、风险仓位、市价、限价、突破、止损、止盈、撤单和平仓均通过统一入口。
- 训练和实时适配器只能提供环境数据，不能重新实现成交计算。
- 市场规则在两个环境中使用同一校验器。
- UI 不直接修改订单、持仓、成交或现金。
- 相同标准输入产生相同结构的执行结果。

### 11.8 确定性成交引擎设置测试

- 佣金、最低佣金、滑点、买卖价差分别设置为非零值时，训练和实时产生相同费用与成交价影响。
- 单根 K 线成交量参与上限为 0 和非零时，训练与实时分别得到一致的整单成交或部分成交结果。
- 同一根 K 线同时触发止损与止盈时，两个环境都服从设置中选择的冲突规则。
- 新训练和新实时市场账户采用创建时的最新配置快照。
- 修改设置后，已有训练、非空实时账户和历史成交保持原结果。
- 页面刷新、应用重启和训练恢复后，配置快照及其版本仍然一致。
- 实时执行路径不存在继续使用隐藏零成本默认配置的调用入口。

### 11.9 执行切换测试

- 旧成交、已平仓记录、历史现金和历史表现升级前后保持一致。
- 有旧待成交委托时不能直接完成切换，撤销后可继续。
- 结转持仓的方向、数量、成本和既有盈亏守恒。
- 新版账户只处理切换游标之后的 K 线。
- 重复切换命令不会重复创建账户、结转持仓或资金事件。
- A 股切换失败不影响美股账户，反之亦然。
- 提交前故障保持旧账户可用，提交后故障进入暂停恢复而不是自动降级重放。

### 11.10 账户生命周期测试

- 每个市场最多一个活动账户。
- 空账户可以原子归档并新建；创建失败时旧账户状态不变。
- 有委托或持仓时不会静默重置。
- 多窗口重复请求只生成一个新账户。
- 归档账户保持只读可查且不混入当前表现。
- A 股、美股和训练账户的创建、结束与重置互不影响。

### 11.11 回归与交付检查

- 受影响控制器和领域测试。
- `npm run typecheck`
- `npm run lint`
- `npm run check:customer-copy`
- `npm run test:unit`
- `npm run build`
- `npm run verify`
- 兼容的 `npm test`
- `git diff --check`
- CodeGraph 复查 `TrainingWorkbench`、`executeBarStep`、live gateway、live ledger、数据库入口和受影响调用者。
- 按 `docs/architecture/smoke-checklist.md` 执行账户、下单、刷新、恢复和失败路径冒烟。
- 使用接近新用户的安装环境验证设置、创建实时模拟账户、首次下单、重启恢复和错误恢复。

## 十二、客户界面与文案要求

- 统一使用“实时筛选”和“实时模拟账户”。
- 明确说明所有订单均为模拟交易，不会发送到券商。
- 设置页分别展示“训练模拟账户”和“实时模拟账户”。
- “确定性成交引擎”继续只展示一套参数，并明确训练与实时模拟均按该设置执行。
- 设置说明明确参数在新训练或新实时市场账户创建时冻结，不再显示“实时观察沿用零成本模型”。
- 账户卡片优先显示当前模式、现金或收益率、账户权益、可用资金和下一步操作。
- 收益率模式的实时表现与训练表现采用相同指标名称和格式。
- 资金账户模式明确标注 A 股或美股账户及币种；全部市场只并列展示，不跨币种求和。
- 账户存在持仓或挂单时，不提供会静默改变计算口径的模式切换。
- 账户冲突、行情未收盘、数据缺失、保存失败和迁移失败都必须说明影响和恢复操作。
- 不向客户展示数据库字段、接口名、迁移命令或内部执行进程。

## 十三、实施约束与风险控制

- 当前 `TrainingWorkbench.tsx` 是共享热状态入口，相关编辑必须串行进行。
- 实施前必须保留工作区已有修改，不覆盖用户正在进行的市场数据改动。
- 本计划涉及数据库 schema 和交易边界，属于独立后续阶段，不应混入现有 Phase 1 的低风险 feature 拆分。
- 数据库迁移必须追加、可恢复、可重复，不允许删除用户实时记录。
- 训练快照、快捷键、市场数据供应商语义和训练回放边界保持不变。
- 不新增第二套实时确定性成交参数，不改变当前设置默认数值；只统一训练和实时的配置来源与执行语义。
- 实时 feature 不得直接访问数据库 runtime；账户状态由服务端应用边界统一管理。
- 不以减少 `TrainingWorkbench` 行数为目标，只有具备独立输入、输出和行为测试的职责才提取。
- 执行切换必须遵守第 9.1 节的版本边界，不得以“统一”为理由重算历史或自动转换旧委托。

## 十四、完成定义

本计划完成时应同时满足：

- 用户能够分别配置训练模拟账户和实时模拟账户。
- 两类账户的模式和资金互不影响。
- 实时资金账户按市场独立；同一市场内所有实时模拟品种共享一份资金和权益。
- 实时表现页在收益率模式下复用训练口径，在资金账户模式下展示当前市场账户的资金、权益和盈亏数据。
- 实时成交链路能够取得完整已收盘 OHLCV。
- 实时账本完整保存共享交易领域字段并兼容旧数据。
- 所有新下单入口使用统一交易命令和共享核心。
- 训练和实时模拟共同使用设置页中的同一套确定性成交引擎参数，并分别保存可恢复的运行快照。
- 实时模拟不再沿用隐藏的零成本、独立成交配置。
- 页面不再包含独立的实时成交、现金和盈亏计算实现。
- 旧训练和旧实时历史仍可读取。
- 所有自动化验证、CodeGraph 影响检查和安装包冒烟通过。
- 既有实时账户按明确版本边界完成切换，历史结果未被重算，旧委托未被自动转换。

## 十五、训练与实时模拟交易统一接口的现状复核与实施计划

本章针对“训练有的交易功能，实时模拟也要有；以后修改交易功能，两边同步生效”作具体收口。复核时间为 2026-09-23。这里的“实时”指 A 股和美股实时行情上的模拟交易，不涉及券商真实下单。第九章已列出账户迁移、公司行为、行情修正等独立风险，本章只说明它们与统一交易入口的依赖关系。

### 15.1 目标与边界

“同一个接口”必须同时满足三个层次：

1. 训练和实时的开仓、平仓、撤单、保护价修改、全平，都形成同一版本的交易命令，使用同一字段含义、校验结果和拒单原因。
2. 数量计算、市场规则、委托生命周期、触发价、跳空过滤、止损止盈、费用、部分成交、现金与权益变化，只在共享交易核心拥有一个权威实现；页面或实时适配器不得各写一套。
3. 训练适配器只提供回放时钟、训练账户状态和快照保存；实时适配器只提供已收盘行情、市场账户和持久化。两者分别返回同结构的交易结果和账户快照。

训练的快进、隐藏未来、训练任务结束、训练日期与价格遮挡属于回放功能，不要求实时复制。实时本阶段仍只覆盖 A 股和美股；外汇、黄金训练能力不因此自动成为实时市场功能。A 股、美股各自的禁止做空、交易单位、T+1 和价格限制继续由市场规则决定，不能以“功能同步”为由绕过。

### 15.2 按当前代码复核的实际状态

| 交易能力 | 当前训练 | 当前实时模拟 | 收口要求 |
| --- | --- | --- | --- |
| 市价、限价、突破委托 | 工作台创建委托，共享执行引擎撮合 | 已通过交易命令提交；已收盘 OHLCV 由 `executeLiveBars` 调用同一个 `executeBarStep` | 委托创建、校验和状态转移也进入共享命令核心 |
| 风险百分比仓位 | 使用现有风险数量计算器和止损价 | 界面隐藏；下单路径将仓位方式固定为固定数量 | 实时启用相同的输入、预览、数量计算和拒单原因；风险基准取所属市场账户 |
| 止损、止盈与图表选价 | 可选价、移动保护线，成交引擎处理保护退出 | 下单控件和保护线在实时模式隐藏；已有持仓可通过部分命令修改 | 统一可见控件、价格校验、保护价修改命令和持仓展示 |
| 跳空不买 | 可设置模式、单位和阈值，并在成交前校验 | 实时界面隐藏；实时成交未使用同一过滤条件 | 委托冻结过滤参数，由共享成交校验在训练和实时调用 |
| 平仓、全平、撤单 | 工作台直接更新训练热状态 | 当前市场账户走实时交易命令；部分旧账本仍可能本地更新 | 所有路径都形成同类命令，旧账本不得产生新的绕行订单 |
| 费用、价差、滑点、同 Bar 冲突、部分成交 | 使用确定性成交引擎设置 | 新账户已有设置快照，行情执行调用共享引擎 | 继续维持一套设置和一个计算实现，补齐跨环境等价测试 |
| 成交与资金写入 | 训练状态和快照 | 页面处理实时行情后仍可通过 `/api/live-state` 提交账户与品种账本变化 | 实时成交及市场资金变化由服务端交易应用服务原子提交 |

现有 `TradingCommand` 类型包含训练和实时环境字段，但 `/api/trading-command` 当前只接受实时模拟命令；训练开仓等操作仍在 `TrainingWorkbench.tsx` 内直接改变本地状态。共用字段类型和 `executeBarStep` 是已完成的基础，尚不等于所有操作已走同一交易接口。

### 15.3 统一命令与返回结果

共享命令明确包含：环境、账户、市场、品种、命令类型、方向、数量或风险参数、委托类型、触发价、止损止盈、行情时间及修订、账户版本、市场规则版本、执行设置版本、命令 ID。命令 ID 在训练会话或实时市场账户内保持幂等，重复提交不能生成第二笔委托。

共享结果包含：接受或拒绝、统一错误码与用户可理解原因、变更后的订单/持仓/成交、账户资金变化、账户版本和处理过的行情证据。UI 根据结果刷新状态，不自行拼装成交或猜测现金。

实现结构建议为一个纯交易命令应用核心、训练会话适配器和实时市场账户适配器。训练不必通过服务端实时账户 API 才能称为“统一接口”；关键是它与实时适配器调用同一命令校验和状态转换核心。实时 API 负责权限、版本、幂等和持久化，不另写交易规则。命令契约要覆盖训练的 FX/黄金市场身份，同时由实时适配器明确限制当前只接收 A 股和美股。

拒单与校验只保留一个权威位置。页面可以做即时输入提示，但服务端和训练适配器都必须以共享核心的最终校验为准；页面提示不能成为实时 API 可绕过的唯一市场规则检查。

### 15.4 交易能力逐项接入

#### 15.4.1 开仓与仓位方式

- 固定数量和风险百分比仓位使用同一个数量计算器、最小交易单位、步长、价格影响和资金/保证金约束。
- 实时收益率模式的风险基准使用所属市场账户的风险计算基准及该账户全部品种的已实现结果，不能只看当前品种，也不能读取训练账户余额。
- 实时资金账户模式使用所属市场共享现金和全部挂单预留计算购买力；同一市场多品种同时下单时由账户版本和服务端事务保证不超额占用。
- 风险仓位必须有有效止损；止损方向不正确、风险预算不足、数量小于市场最小单位时，训练与实时返回同一类拒单原因。
- 市价、限价和突破单共用订单对象、触发价合法性、创建时间与最早可执行 K 线规则。实时委托按其已收盘日线证据处理，不把未收盘报价当成确定性成交。

#### 15.4.2 保护价格与图表交互

- 训练和实时显示相同的止损、止盈与触发价输入；实时可以从图表选价，并看到已持仓的保护线。
- 图表选价只产生标准化的修改命令，不直接修改持仓数组。止损只能向允许方向收紧、保护价必须位于正确价格侧等规则由共享核心执行。
- 新委托携带保护价并在成交后完整进入持仓；部分成交、撤单、页面刷新和账户恢复后，剩余委托与已成交持仓的保护状态必须一致。
- A 股 T+1 等市场规则可能使保护性退出暂时不可执行，此时保留保护意图并显示待处理状态，不虚构当日平仓。

#### 15.4.3 跳空不买与其他条件

- 训练和实时使用同一套高开/低开阈值、百分比/价格单位和比较边界。
- 过滤条件在提交委托时随订单冻结；以后修改偏好设置不改变已挂委托。
- 实时每根待执行 K 线取可核验的前一根已收盘价格，按与训练相同的过滤函数处理。缺少前收盘或数据口径不明确时明确标记证据不足，不以零或当前价替代。
- 因跳空过滤未成交时，委托是继续挂单、取消还是拒绝，必须沿用训练现有语义并在两边测试同一结果。

#### 15.4.4 平仓、全平和撤单

- 单仓平仓、当前品种全平、当前市场账户全平分别是明确命令，作用范围由账户与品种身份决定。
- 撤单、修改保护价和全平都通过共享命令核心校验订单与持仓状态；重复操作返回幂等结果或同一类状态冲突。
- 训练回放可能预知下一交易日的时间戳，实时不能预知尚未出现的 K 线；两边共用“最早可执行交易时段”的规则，实时由未来第一根合格已收盘 K 线兑现。
- 实时市场账户全平仅作用于所选市场，不跨人民币与美元账户执行。

#### 15.4.5 成交、资金和配置

- 两个适配器都把完整合法的 K 线与冻结的确定性成交设置送入同一个 `executeBarStep`，市场规则校验及跳空过滤使用同一组回调/领域函数。
- 佣金、最低佣金、滑点、价差、成交量参与上限、部分成交及同 Bar 冲突策略保持一个权威实现。旧实时历史不因接入而重算。
- 训练将结果写入会话状态和既有快照；实时在服务端同一事务写入账户、品种、订单、成交、拒单、行情游标及命令回执。
- 页面行情刷新只请求处理和展示结果，不再提交可任意覆盖交易账本的账户/品种快照。观察列表等非交易偏好可以保留独立保存路径。

### 15.5 旧实时账本与切换

- 已有不绑定市场账户的品种账本继续只读展示，不把它的现金或订单自动并入新市场账户。
- 移除“旧品种账本首笔委托直接本地挂单”的例外；用户先创建或切换到明确的市场账户，再通过统一命令下单。
- 旧待成交委托、未平持仓和执行版本按第 9.1 节逐市场预检和结转。已成交历史保持原值，旧委托不自动转换为新版触发规则。
- 表现列表以一致的实时模拟标的样式兼容呈现未绑定和已归档账本，不标注“历史/旧记录”；可交易范围仍由账户关联状态在内部判断。未关联活动账户的账本保持只读，用户尝试交易时说明需要先关联账户；账户尚未就绪、版本冲突或迁移失败时阻止下单并提供恢复动作。

### 15.6 推荐实施顺序

1. 固化当前训练与实时的订单样本和可见结果，建立市价、限价、突破、风险仓位、保护价、跳空过滤、撤单、平仓与全平的能力矩阵及对照测试。以可控 OHLCV 和两个市场账户构造基线，不依赖真实外部同步。
2. 定义共享交易命令、账户快照、订单结果和错误码，补足训练市场身份与实时市场限制。先让训练适配器和实时适配器都通过该契约运行，保持训练快照和实时账本旧数据可读。
3. 将订单创建、数量和风险计算、市场校验、委托状态变更、保护价、跳空过滤及资金预留收进共享核心。训练和实时页面只保留表单输入与结果展示，移除各自维护的重复业务分支。
4. 同步开放实时下单控件：风险百分比、单笔风险、止损止盈、图表选价、保护线与跳空不买。逐项验证键盘/鼠标、正常、加载、拒单、恢复和窄窗口状态。
5. 将实时已收盘行情的执行与账户写入收进服务端应用服务；以账户版本、行情游标和命令 ID 原子提交。实时页面只读取返回快照，不再自行修改成交与资金。
6. 按第 9.1 节迁移活动市场账户，关闭旧品种账本的本地下单入口；完成 A 股、美股多标的并发、重启、数据修正和公司行为回归。
7. 删除没有调用者的重复校验、旧实时撮合和交易账本整对象写入路径；保留必要的只读兼容与非交易数据保存入口，更新架构不变量和首次使用说明。

第 3 步是“以后改一处，两边同步”的关键。只统一页面布局或 HTTP 路由，而让训练、实时分别计算数量、拒单和资金，不能满足目标。共享核心每次变更都要跑训练与实时的同输入对照测试，防止新规则只影响一个环境。

### 15.7 主要影响位置

- `web/app/lib/tradingCommands.ts`：交易命令与错误结果契约。
- `web/app/lib/executionEngine.ts`、`web/app/lib/liveExecutionAdapter.ts`、市场规则、风险仓位和跳空过滤模块：共享计算与环境输入。
- `web/app/components/TrainingWorkbench.tsx`：训练和实时命令提交、下单控件、图表交互与页面组合；此共享文件串行修改。
- `web/app/features/live/liveGateway.ts`、`web/app/api/trading-command/route.ts`、`web/db/live-command.ts`：实时命令适配与应用服务。
- `web/app/api/live-state/route.ts`、`web/db/live-ledger.ts`：将交易写入收敛为受版本保护的服务端结果，保留历史兼容读取。
- `web/app/features/settings`：继续提供单一确定性成交引擎设置，分别冻结训练和实时运行快照。
- 训练、实时命令、执行引擎、账本与客户界面测试。

### 15.8 验收门槛

- 在相同市场规则、账户模式、冻结执行设置和完整 K 线输入下，训练与实时对开仓、撤单、修改保护价、平仓和全平给出同结构的订单及资金变化；环境只改变时钟与持久化位置。
- 实时 A 股、美股在各自市场允许的范围内可使用训练的固定数量、风险百分比、市价、限价、突破、止损止盈、跳空不买、部分成交、撤单和平仓能力。
- 实时没有本地下单或页面自行生成成交/现金变化的例外路径；旧品种账本只读。
- 改动一项共享交易规则时，训练和实时对照测试均感知该变化；不存在只在 `liveMode` 分支里实现的第二套计算。
- 两个市场账户资金独立、市场内多品种共享，账户版本冲突和重复行情刷新不会产生重复成交或超额购买力。
- 训练历史快照、实时既有成交、公司行为事件和行情修订证据可读，切换前的历史结果不重算。
- 项目要求的局部测试、类型检查、lint、客户文案检查、单元测试、构建、`npm run verify`、兼容 `npm test`、CodeGraph 影响检查和安装包首次使用冒烟均通过。

当前尚无必须由用户补充才能写实施代码的产品决策；“训练有的功能”按本章限定为受当前实时市场支持的模拟交易能力。若未来要把实时市场扩展到外汇、黄金或分钟线，应另行明确行情与账户边界，再复用同一交易核心。
