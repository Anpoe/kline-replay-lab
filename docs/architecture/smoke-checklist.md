# Phase 0–1 用户流程冒烟清单

## 使用规则

- 默认使用本地数据和隔离的开发数据库；不把真实供应商同步、生产数据库或不可逆清理作为默认步骤。
- 每次 feature 迁移后复测受影响项目；Task 10 再完整执行一遍。
- 每项记录前置条件、操作、预期结果、服务端边界和实际结果。页面加载、错误展示和恢复入口也属于可验证行为。
- “通过”表示用户可见结果和关键请求边界符合现有行为；“阻塞”必须附原因和下一步，不可静默跳过。

## 基线环境

| 项目 | 基线值 |
| --- | --- |
| 工作区 | `C:/Users/Axis/.codex/worktrees/kline-phase-0-1` |
| 分支 | `codex/kline-phase-0-1` |
| Web 命令 | `cd web; npm run dev` |
| 默认地址 | `http://localhost:3101` |
| Windows 入口 | `KLineTrainingCamp.ControlPanel.exe`（两个 BAT 仅兼容转发） |
| 后台服务 | `127.0.0.1:3100` 行情、`127.0.0.1:3102` 自动更新 worker |
| 数据策略 | 优先本地测试数据；外部供应商只验证错误/恢复入口 |
| 基线日期 | 2026-08-20 |

## 流程清单

### 0. 托盘面板启动与后台服务

- 前置：已运行 `launcher/build-native-control-panel.ps1` 生成 `KLineTrainingCamp.ControlPanel.exe`、Node.js `>=22.13.0`；不执行真实外部同步。
- 操作：双击 `KLineTrainingCamp.ControlPanel.exe`，观察面板的 3100/3101/3102 三个状态卡和“当前后台工作”；关闭窗口后从系统托盘重新显示，再点击“打开 WebUI”。勾选开机后台启动后，确认 Startup 快捷方式目标为 EXE 且参数为 `--hidden`。
- 预期：任务栏只出现一个面板图标，不出现 cmd 或 PowerShell 窗口；面板保持托盘常驻；三个本机服务按顺序就绪；浏览器只在点击按钮后打开；日志能区分行情服务、Web 服务和后台 worker。worker 按运行软件这台机器的系统本地日期执行启动检查或设定时间后的定时检查，同一天重开面板不会重复执行数据库更新；若在定时点之后启动，首次 worker 检查应立即执行。
- 请求边界：后台 worker 只访问 `127.0.0.1:3101/api/*`；3102 只绑定回环地址；不自动打开公网防火墙端口。
- 恢复：模拟 Web 服务未就绪或端口冲突时，面板显示明确错误，重启按钮可重新尝试；停止面板不会伪造每日任务成功。

### 1. 打开训练页和初始加载

- 前置：启动 Web 开发服务器，使用无痕/新浏览器上下文。
- 操作：打开 `/`，等待首屏稳定，观察训练页标题、导航和空数据/加载状态。
- 预期：页面成功渲染 K 线训练工作台；没有未处理的运行时错误；无数据时给出可理解的数据入口。
- 请求边界：只允许页面初始化所需的现有 API；不触发真实供应商同步。
- 基线结果：Task 4 执行时填写。

### 2. 加载或切换本地市场数据

- 前置：本地数据目录或仓库自带测试数据可用。
- 操作：选择市场、品种和周期，打开数据入口并加载一段已有数据；再切换到另一个可用组合。
- 预期：加载状态结束后 K 线和训练上下文更新；切换不会改写订单规则或产生跨数据源错误。
- 请求边界：训练页通过现有 candles/local-data API 获取标准化 K 线；供应商原始响应不进入 UI 训练状态。
- 基线结果：Task 4 执行时填写。

### 3. 移动回放游标、播放和暂停

- 前置：已加载至少一段本地 K 线。
- 操作：单步推进游标，点击播放，等待数根 K 线，再点击暂停。
- 预期：游标按既有边界单调推进；播放/暂停状态和当前 K 线一致；没有跳过或重复执行 Bar 的可见异常。
- 请求边界：纯客户端训练动作不应新增设置、历史或实时扫描请求。
- 基线结果：Task 4 执行时填写。

### 4. 下单、撤单和保护条件

- 前置：处于可下单的训练 Bar，账户和品种规则已加载。
- 操作：创建一个符合规则的订单，检查挂单，再撤单；另建一笔带止损/止盈或保护条件的订单并推进回放。
- 预期：订单状态、持仓、现金/权益和保护条件符合现有交易规则；撤单和触发结果可见且无重复成交。
- 请求边界：动作由训练核心处理，不由 settings/review/live feature 直接写入。
- 基线结果：Task 4 执行时填写。

### 5. 设置与偏好

- 前置：训练页已打开。
- 操作：打开设置，修改显示/训练偏好，保存；刷新页面后再次打开设置；再提交一个可恢复的无效输入或断网保存场景。
- 预期：保存成功显示既有反馈，刷新后值保持；无效或失败保存显示错误并保留恢复入口；回放游标、订单和持仓不被设置操作隐式修改。
- 请求边界：只调用现有 preferences/provider-settings 等配置 API，不直接访问数据库运行时。
- 基线结果：Task 4 执行时填写。

### 6. 保存快照、查看历史和恢复快照

- 前置：已有训练上下文，最好完成至少一个可保存状态。
- 操作：保存快照/会话，打开历史列表，进入复盘指标；选择一个历史状态恢复，恢复后继续单步回放。
- 预期：历史列表出现新记录；复盘指标和证据可见；恢复顺序正确，恢复后训练状态可继续且不出现半恢复页面。
- 请求边界：使用现有 sessions/snapshots/analysis API；review UI 不直接调用 getRawDb 或 ensureSchema。
- 基线结果：Task 4 执行时填写。

### 7. 查看复盘指标

- 前置：存在有交易或无交易的历史会话。
- 操作：打开复盘，切换筛选/排序，分别查看有证据和缺失证据的指标。
- 预期：指标计算使用现有确定性公式；空集、平仓/未平仓和缺失证据提示正确，不静默伪造数据。
- 请求边界：指标加载走现有 snapshot analysis/session API，公式仍由 `reviewMetrics.ts` 提供。
- 基线结果：Task 4 执行时填写。

### 8. 数据维护和错误恢复

- 前置：本地数据维护入口可打开；外部供应商凭证可保持为空或使用明确的测试配置。
- 操作：打开数据管理，查看覆盖范围/目录；选择不会写入生产数据的维护入口；模拟无凭证、网络失败或空结果；观察重试/取消/错误展示。
- 预期：本地状态和覆盖范围可读；失败显示明确原因与恢复入口；默认冒烟不发起真实外部同步。
- 请求边界：维护请求只从 market-data 边界发起；错误不覆盖当前训练行情窗口。
- 基线结果：Task 4 执行时填写。

### 9. 实时扫描、刷新和结果跳转

- 前置：本地扫描数据或可控的空结果条件可用。
- 操作：打开实时扫描，提交筛选条件，观察加载/空结果/错误状态；刷新结果；选择一条结果跳转到训练上下文，然后返回原训练状态。
- 预期：扫描结果、刷新频率、取消/重试和空结果提示保持现有行为；结果跳转只在明确选择后发生；刷新不覆盖正在进行的回放。
- 请求边界：扫描使用现有 live-scan API；live feature 通过 shell 交接选择事件，不直接改回放、订单或持仓。
- 基线结果：Task 4 执行时填写。

### 10. 个人 SOP 与严格模式

- 前置：存在至少一组历史训练记录；其中至少一组组合达到 15 笔已平仓样本，另准备一组 14 笔样本用于验证门槛。
- 操作：打开 SOP 页面，检查美股日线、A 股日线和 EURUSD 5 分钟的样本进度；查看候选的入场语境、计划习惯、价格/成交量/成交额/市值资料和持仓 K 线；采用一条可采用候选并打开交易纪律设置。
- 预期：14 笔组合不能采用；达到 15 笔且正期望的候选才显示可采用；候选不会跨市场、周期或品种凑样本；当前规则不会因新候选出现而静默替换。
- 操作：分别切换严格模式、事前规划卡、规划字段、内置 SOP、个人表现 SOP 和超限自动平仓开关，保存后用缺失/匹配/不匹配的决策卡尝试开仓，再在回放中推进到持仓上限之后。
- 预期：规划卡只拦截被选中的字段；内置 SOP 和个人表现 SOP 独立生效；价格、成交量、成交额等有资料的条件不匹配时拦截，资料缺失只提示；持仓超限默认提示，严格模式和自动平仓同时开启时沿用既有订单队列及 A 股 T+1 规则排队平仓。
- 请求边界：个人 SOP 计算使用历史会话和快照分析；严格模式检查只发生在训练开仓入口；自动平仓只作用于本地回放，不发起真实交易或绕过市场交易规则。
- 基线结果：Task 4 执行时填写。

## 基线执行记录

### 2026-08-20（Phase 0）

- 自动化入口：Playwright CLI，使用 `http://localhost:3102`；默认端口 `3101` 已被其他进程占用，因此 Vinext 自动切换到 `3102`。
- 数据状态：首屏和数据页使用本地可用数据；数据页显示 A 股 6,928 个品种、25,480,445 根 K 线、4 个周期、0 个已知异常。
- 通过：1 初始加载；2 数据页和本地行情入口；3 下一根、播放、暂停；4 买入委托创建/撤单以及图表止损设置/清除；5 设置保存、刷新后恢复并将临时值还原；6 自动保存的训练出现在历史、复盘可打开并通过“继续训练”恢复到 `12 / 40`；7 复盘指标和缺失交易提示；8 数据维护入口和离线失败提示；9 实盘筛选弹窗、离线失败提示、恢复入口和关闭。
- 错误路径：将浏览器置为 offline 后，实盘筛选显示 `Failed to fetch` 和“使用本地现有最新数据筛选”恢复入口；控制台唯一错误为预期的 `net::ERR_INTERNET_DISCONNECTED`，恢复 online 后关闭弹窗成功。
- 外部副作用：未执行真实 Tushare/Alpaca/FX 同步；测试产生的临时训练自动保存于隔离 worktree 的开发数据环境，不写入原工作区代码。
- 阻塞：无。结果作为 Phase 0 基线保存；迁移后按同一编号复测。
- 代码级护栏：`web/npm run verify`、`web/npm test` 均通过，作为 UI 冒烟前置条件；它们不替代本清单。

## 迁移后记录模板

每个 feature 完成后追加日期、改动 feature、复测编号、实际结果、请求边界和未解决风险。不得重写基线标准；如预期行为发生变化，必须先更新设计/spec 并说明原因。

## 迁移后执行记录

### 2026-08-20（Phase 1 完成）

- 环境：隔离 worktree，Vinext 开发服务 `http://localhost:3102`；浏览器控制台初始加载无错误。
- 通过：1 首屏加载；2 数据页和本地数据概览（6,928 个品种、25,480,445 根 K 线、4 个周期、0 个已知异常）；3 下一根、播放、暂停；4 市价买入挂单并撤单；5 设置面板保存 A 股默认数量 `100 → 120`，重新打开读取 `120`，随后还原为 `100`；6 复盘入口、历史列表和“查看复盘”展示 `本次已实现收益率`；9 实时扫描弹窗、离线失败提示、恢复入口和关闭。
- 保护条件：Task 4 基线已完成图表止损设置/清除；本阶段没有修改图表和交易保护实现，迁移后主流程继续可见，未引入新的保护条件请求。
- 离线错误路径：浏览器 offline 后，实时扫描显示 `Failed to fetch` 和“使用本地现有最新数据筛选”；控制台出现的 `net::ERR_INTERNET_DISCONNECTED` 仅对应预期的 `/api/cn-maintenance` 和 `/api/live-scan` 失败，无其它错误。
- 请求边界：设置、市场数据、复盘和实时扫描 UI 的迁移没有新增真实供应商同步；外部同步未执行，数据页和训练数据使用隔离开发数据。
- 未解决风险：`TrainingWorkbench` 仍承载训练热状态、部分页面级 API 适配和跨 feature 副作用；该债务已记录在各 feature README，留给后续按 gateway/命令状态模型继续拆分。
