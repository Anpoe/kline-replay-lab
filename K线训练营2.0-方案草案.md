# K线训练营 2.0：产品与技术方案草案

> 版本：v0.31（部分成交、风险仓位与图表保护单）
> 更新日期：2026-08-11
> 状态：纯本地纵向原型可运行；Sites 托管配置已移除，正在向可用 MVP 推进

## 0. 一句话定义

一个面向个人交易训练的多市场 K 线回放系统：使用历史 OHLCV 数据隐藏未来、逐根推进，允许模拟下单和图表标记，并把每次训练沉淀为可检验的策略规则与复盘报告。

它不是“看历史图 + 算盈亏”的播放器，而是下面这个闭环：

```text
选择训练任务 → 隐藏未来逐 K 决策 → 模拟成交 → 标记与记录理由
        ↑                                      ↓
   策略新版本 ← 小样本验证 ← 提出可检验的改进 ← 结构化复盘
```

## 1. 已知需求与本方案的默认理解

### 已知需求

- 接入美股、A 股、外汇、黄金等历史数据。
- 不需要实时数据。
- 第一阶段只处理单根 K 线数据，即 OHLC，可选 Volume/Turnover，不依赖 Tick 和盘口。
- K 线回放，逐根显示未来数据。
- 在回放过程中模拟交易。
- 支持画线、图形和 K 线语义标记。
- 训练结束后自动总结，并帮助优化策略。
- Replay 的操作体验参考 FastBull，但复盘能力要明显更强，同时补上 A 股。
- 市场优先级：A 股和美股第一优先，外汇和黄金第二批紧随其后。
- 核心训练周期：5 分钟、1 小时、日线；周线必须支持查看和回放上下文。
- 建立可长期积累的详尽 K 线历史数据库，而不是每次训练临时请求行情；现阶段不建设基本面、新闻、公司行动明细等其他数据域。
- 随机训练和新建 Replay 训练可以按历史 K 线形态筛选起点；第一版采用严格的 yes/no 条件，不做相似形态搜索和主观评分。
- 后期需要 Android App；第一版暂不开发云同步，但数据模型和账号边界必须为多设备同步预留。

### 默认假设

- 第一版是个人使用、桌面优先、中文优先，但从第一天按响应式布局和触摸操作设计。
- 重点是主观交易与价格行为训练，而不是程序化策略回测平台。
- 第一版不要求盘中更新；收盘后或按需批量更新历史数据库即可。
- 外汇的 `volume` 可以为空或使用 tick volume，不能与股票成交量直接比较。
- “详尽数据库”当前特指多市场、多周期、尽可能深历史的 K 线库及其质量/来源信息。
- 云同步暂缓；行情和用户数据先本地保存，但主键、版本号和删除标记采用可同步设计。
- 所有总结首先用于教育和交易流程改进，不承诺盈利，也不直接替用户做买卖决定。

## 2. 产品原则

1. **评价决策质量，而不只评价结果**：一笔盈利交易也可能是违规或运气，一笔亏损交易也可能严格执行了高质量计划。
2. **杜绝未来函数**：训练时能看到什么、何时提交决策、成交在哪根 K 线，都必须可审计。
3. **先结构化，后使用 AI**：盈亏、R 倍数、MFE/MAE、最大回撤等由确定性程序计算；AI 只负责归纳、解释和提出可验证假设。
4. **策略优化必须版本化**：AI 不能悄悄“改策略”。每项修改都生成候选规则、证据、样本范围、潜在副作用和验证任务。
5. **多市场共用一套训练内核，市场规则分开配置**：A 股 T+1、涨跌停、整手规则不能被外汇或美股的成交逻辑覆盖。
6. **本地优先、可迁移、可同步**：原始行情、标记、交易记录和策略版本都可导出；本地是当前真相来源，但对象身份和版本从一开始兼容未来云同步。
7. **桌面与 Android 共用领域模型**：回放、成交、统计和策略规则不得依赖桌面 UI，未来手机端复用同一数据格式和业务逻辑。

## 3. FastBull Replay：借鉴点与改进点

### 值得借鉴

- 在原 K 线页面直接进入 Replay，不需要跳到另一个复杂模块。
- 进入回放后立即出现统一的底部控制条。
- 支持按 K 线、时间、随机位置选择起点。
- 训练模式与测试模式分开；测试模式可以选择 1/2/3/6 个月及 1/3/5 年等训练区间。
- 支持倍速、播放、逐根前进、停止/跳转。
- 模拟账户、买卖面板、当前持仓和历史订单与图表共处一屏。
- 初始资金、点差、杠杆、强平线、佣金、隔夜费可配置。
- 历史训练可重新打开。

### 我们需要做得更好的地方

- FastBull 当前总结主要是盈亏、胜率、交易次数、历史记录和测试排行；缺少对入场背景、位置、风险、执行一致性和重复错误的解释。
- 加入 A 股数据及独立的 A 股成交规则。
- 标记不只是画线，还要能表达“趋势/震荡/突破模式、关键位置、入场理由、无交易原因”等语义。
- 每笔交易保存下单前计划，防止训练结束后用结果倒推理由。
- 支持策略版本对比，分清“策略没优势”和“执行没遵守策略”。
- 对仅有 OHLC 的同 K 线止盈/止损冲突给出明确、保守、可配置的处理方式。

## 4. 核心训练流程

### 4.1 创建训练

用户选择：

- 市场、品种、周期。
- 起点：指定 K 线、指定日期、随机起点、系统题库。
- 模式：自由训练、盲测、错题重练。
- 策略版本。
- 初始资金、仓位模型、佣金、点差、滑点、杠杆等。
- 是否隐藏品种名、日期和价格绝对值。
- 训练长度：K 线数量、交易日数量或自然时间。

系统保存一份不可变的 `data_snapshot_id + random_seed + rule_profile`，保证以后可以完全复现。

### 4.2 每根 K 线的训练动作

在当前 K 线收盘后，用户可以：

- 标记市场状态：趋势、宽通道、震荡区间、突破模式、突破、回调、反转尝试。
- 标记主导方：多方、空方、平衡/无明显优势。
- 标记位置：区间上沿/中部/下沿、前高低、均线、趋势线、开盘价、整数位等。
- 写交易假设和至少两个独立理由。
- 填入计划：方向、触发价、失效点、止损、第一目标、移动/分批逻辑、放弃条件。
- 下模拟订单，或明确记录 `no-trade`。
- 揭示下一根 K 线，或自动播放。

### 4.3 结束训练

系统先生成确定性统计，再生成可解释总结：

- 发生了什么。
- 哪些决策符合策略。
- 哪些错误重复出现。
- 哪些场景可能存在优势。
- 哪条策略规则值得形成新版本。
- 下一次应该做什么定向训练。

## 5. 功能模块

### 5.1 训练首页

- 继续上次训练。
- 快速创建：随机 100 根、指定品种/日期、错题重练。
- 训练日历与连续训练天数。
- 最近暴露的三个问题，如“震荡区间中部入场过多”。
- 策略版本表现，但默认显示样本量和置信提示，不只显示收益率。

### 5.2 Replay 工作台

建议布局：

```text
┌ 品种 / 周期 / 数据快照 / 模式 / 策略版本 / 保存 ┐
├ 画线与标记 ┬──────── K 线主图 ────────┬ 决策卡 ┤
│            │                           │ 背景   │
│            │                           │ 理由   │
│            │                           │ 计划   │
├────────────┴ 回放控制条 ──────────────┴────────┤
│ 持仓 / 订单 / 成交 / 事件日志 / 训练笔记        │
└───────────────────────────────────────────────┘
```

回放控制条至少包含：

- 回到起点、上一根、下一根。
- 播放/暂停。
- `0.5x / 1x / 2x / 5x`。
- 一次推进 1 根、5 根或到下一交易日。
- 随机跳转仅用于新训练；已有训练中禁用，避免污染记录。
- 当前进度、已用时间、当前 K 线编号。

主图指标第一版已接入 `MA` 与 `EMA`：入口位于桌面端顶部周期按钮右侧，手机端收纳在周期工具面板；两组指标可独立开关、增删并修改 1–500 的周期参数，每组最多 6 条。指标只使用 Replay 当前已经揭示的 K 线计算，配置保存在本机，切换品种、推进下一根或刷新后仍可沿用。

### 5.3 图表标记

标记分为三层，避免“所有东西都是一条线”：

1. **绘图层**：趋势线、水平线、矩形、通道、斐波那契、箭头、自由笔。
2. **语义层**：市场状态、关键位置、信号 K、突破/失败突破、第二次入场、楔形、区间中部、无交易等标签。
3. **交易层**：入场、加减仓、止损、目标、退出、计划变更。

每个标记必须锚定 `symbol + timeframe + bar_timestamp`，价格型标记再保存 price。标记还应保存：

- 创建时用户已经看到的最后一根 K 线。
- 创建前/后文字说明。
- 来源：用户、系统规则、AI 建议。
- 可见范围：仅本次训练、该品种所有训练、策略模板。

这样回看时可以恢复“当时的视角”，不会意外显示未来才添加的注释。

### 5.3.1 历史形态筛选训练（第一版已接入）

形态筛选不是一个单独的选股页面，而是训练起点生成器：

1. 左侧“形态”打开与设置相同的弹出视图，用来管理形态预设和阈值。
2. 在“随机训练”或“新建 Replay 训练”中勾选一个或多个预设。
3. 系统只使用候选 K 线及其之前的数据判断是否命中，绝不读取后续 K 线。
4. 命中后把该 K 线作为 Replay 起点，未来数据继续隐藏；训练长度照常控制自动结束和平仓。
5. 训练任务保存所选预设、实际命中的预设和命中时间，供复盘与完全复现使用。

第一版内置十类布尔预设：区间突破、上升趋势、上升趋势突破、趋势回调、波幅收缩、看涨吞没、看跌吞没、突破回踩、失败突破、长下影线。用户可以调整窗口、容差、均线周期、成交量倍数、影线比例等参数，也可以复制内置预设形成自己的版本。

组合逻辑固定为：

- **一个预设内部：AND**。例如“区间突破”中的回看窗口、最小突破幅度和成交量确认必须同时满足。
- **多个被选预设之间：OR**。例如同时勾选“突破回踩”和“长下影线”，命中任意一种即可进入候选集。
- **不使用评分**。结果只有命中/不命中，避免把阈值筛选误解成质量预测。

为了避免同一段走势被重复抽到，训练设置中提供“相邻命中冷却 K 线数”；随机训练还可以限制最多检查的候选数据集数量，在命中率和启动速度之间取舍。形态参数保存在本机，通用扫描规则留在“设置 → 训练设置”中。

当前实现采用按需扫描：新建 Replay 只扫描所选品种和周期；随机训练按设置范围逐个检查有限数量的候选数据集。后续数据量继续增长时，再增加离线形态索引与增量更新，而不改变训练任务格式。

### 5.3.2 实盘筛选（第一版已接入）

实盘筛选复用同一套形态预设，但与 Replay 的历史起点扫描分开：它只判断所选市场最近一个完整收盘交易日，不读取盘中尚未完成的日 K，也不提供实时下单。

1. 训练页顶部在“随机训练”左侧提供独立“实盘筛选”入口，手机端使用同一入口和配置。
2. 第一版支持 A 股与美股 `1d`；A 股使用 TDX 基础库叠加 Tushare 增量，美股只使用 Alpaca SIP 覆盖。
3. 用户确认后先经过行情新鲜度门：A 股自动启动每日增量，美股自动补齐至 Alpaca 交易日历确认的最近收盘日；更新完成后才开始筛选。
4. 更新失败时明确显示原因，并允许用户主动选择“使用本地现有最新数据筛选”，但结果必须标明实际行情日期。
5. 可选条件包括形态预设、最低/最高收盘价、20 日平均成交量、排序方式和结果数量。形态预设之间为 OR；价格和流动性条件与形态结果之间为 AND。
6. 结果显示实际行情日、证券代码与名称、命中形态、收盘价、当日涨跌幅及平均成交量；点击结果打开该品种最新日线。

第一版不是实时行情终端：不承诺盘中更新，不直接连接券商下单，也不会把实盘筛选结果自动记为一场训练。后续若增加盘中 K 线，将另设“数据截至时间”和未收盘 K 线标记，避免把盘中形态与收盘形态混用。

### 5.4 模拟交易

#### MVP 订单类型

- 市价单。
- 限价单。
- 止损触发单。
- 止损和止盈。
- 平仓；第一版可以先使用净持仓模型。

#### 防止未来函数的成交规则

- 用户在第 N 根 K 线收盘后提交市价单，默认在第 N+1 根开盘价成交，再叠加点差/滑点。
- 限价或止损单只用下一根及之后的 OHLC 判断是否触发。
- 如果一根 K 线同时触及止盈与止损，仅凭 OHLC 无法知道先后顺序。默认使用**保守规则**：按对用户不利的一侧先成交，并将该笔标记为 `intrabar_ambiguous`。
- 后续可以增加三种可选策略：保守、乐观、随机路径；报告中必须单独显示受影响交易数量。
- 不允许用当前 K 线的最高/最低价提交并成交于同一根 K 线，除非使用更小周期数据进行验证。

#### 市场规则配置

| 市场 | 第一版建议规则 |
|---|---|
| A 股 | 默认多头、T+1、100 股整手、交易时段/节假日、涨跌停；价格用未复权成交价，复权图只用于观察 |
| 美股 | 多头；做空作为可选开关；支持常规时段，盘前盘后暂不做或单独标记 |
| 外汇 | 多空、点差、杠杆、隔夜费；数量按手或名义金额 |
| 黄金 | XAUUSD 按外汇贵金属合约处理；上海黄金需使用独立品种规格 |

所有规则放在 `market_rule_profile` 中，不写死在回放引擎里。

### 5.5 复盘与总结

总结分四层：

#### A. 事实层（程序计算）

- 净利润、手续费、滑点成本。
- 胜率、平均盈利/亏损、盈亏比、期望值。
- 总 R、平均 R、Profit Factor。
- 最大回撤、连续亏损、恢复时间。
- MFE、MAE、持仓 K 线数。
- 入场后 1/3/5/10 根 K 的表现。
- 按市场状态、位置、方向、时间段、标签、策略版本拆分。
- `intrabar_ambiguous` 和数据缺口对结果的影响。

#### B. 流程层（规则评分）

每笔交易检查：

- 是否先分类市场状态。
- 是否在有利位置，还是在区间中部追单。
- 是否有至少两个独立理由。
- 是否等到信号 K 收盘。
- 风险是否在入场前定义。
- 止损是否对应真实失效点。
- 目标是否适合当前市场状态。
- 是否遵守策略版本和仓位规则。
- 退出来自结构变化还是情绪化操作。

#### C. 模式层（统计归纳）

示例：

- “最近 42 笔中，`middle-of-range` 有 11 笔，期望值 -0.38R；在区间边缘的 17 笔为 +0.21R。”
- “你的策略在趋势延续中表现尚可，但亏损集中在第一次逆势反转尝试。”
- “盈利交易中有 36% 没有事前计划，结果好但不可重复。”

#### D. 策略改进层（AI 建议）

每项建议必须采用以下结构：

```yaml
observation: 观察到的重复现象
evidence: 样本数、时间范围、关键指标和反例
candidate_rule: 可执行、可判断的新规则
expected_effect: 可能改善什么
side_effect: 可能错过什么机会
validation_task: 下一批独立样本如何验证
confidence: low | medium | high
```

AI 不直接把建议合并到正式策略。用户点击“创建候选版本”后生成 `v1.3-draft`，再用未参与归纳的训练样本做验证，避免过拟合。

### 5.6 策略实验室

一份策略不是一段散文，而是一组可检查字段：

- 适用市场和周期。
- 市场背景过滤器。
- 允许的位置。
- Setup 与触发条件。
- 至少两个确认理由。
- 仓位和单笔风险。
- 初始止损与失效条件。
- 第一目标、分批和 runner 规则。
- 明确的 no-trade 条件。
- 示例图、反例图。

支持：

- 版本历史和变更原因。
- 两个版本的样本外对比。
- 将报告中的错误一键生成专项训练集。
- 策略卡导出为 Markdown/JSON。

### 5.7 K 线回看库与错题本

- 按品种、周期、日期、策略、标签、结果、流程得分搜索。
- 保存“决策前截图”和“完整走势截图”。
- 回看时先恢复当时可见范围，再允许揭示结果。
- 错题不只收录亏损：违规盈利、没有执行的好机会、无交易错误都要收录。

## 6. 数据方案

### 6.1 统一数据模型

```ts
type Candle = {
  instrumentId: string
  market: 'CN' | 'US' | 'FX' | 'METAL' | string
  timeframe: '5m' | '1h' | '1d' | '1w' | string
  timestamp: number       // UTC 毫秒；另存交易所时区
  tradeDate: string       // 品种所属市场的交易日期
  open: number
  high: number
  low: number
  close: number
  volume?: number         // 外汇可为空或 tick volume
  turnover?: number
  sessionId?: string
  isAdjusted: boolean
  adjustmentType?: 'none' | 'forward' | 'backward'
  source: string
  snapshotId: string
  dataVersion: number
  qualityFlags?: string[]
}
```

除 K 线外，只维护让 K 线可正确读取所必需的最小目录：

- 品种代码、名称、市场、交易所、价格精度和时区。
- 生成 1h/周线所需的基础交易时段配置。
- 数据来源、覆盖区间、导入时间、校验状态和授权范围。

### 6.2 “详尽数据库”的具体范围

当前阶段只建设 K 线数据库。这里的“详细”指覆盖广、历史深、周期完整、来源可追溯，不指加入更多非 K 线数据。

| 当前数据表 | 主要内容 | 用途 |
|---|---|---|
| `candle` | OHLC、Volume、Turnover、周期、复权类型、来源、版本、质量标记 | 回放、图表和模拟成交 |
| `instrument_catalog` | 代码、名称、市场、交易所、时区、价格精度 | 搜索和正确展示 K 线 |
| `candle_coverage` | 每个品种/周期的起止时间、数量、缺口、数据源、校验时间 | 判断数据库是否完整 |
| `data_snapshot` | K 线版本、覆盖区间、内容哈希和导入时间 | 完全复现历史训练 |

#### 明确暂缓的数据

- 财务报表、估值、行业和基本面指标。
- 新闻、公告、宏观事件和财经日历。
- Tick、逐笔成交、盘口和 Level 2。
- 持仓、资金流、龙虎榜、期权链等衍生数据。
- 分红、拆股、送转等公司行动明细库；当前只接收数据源已经生成的未复权/前复权/后复权 K 线序列。

#### 时间周期的存储策略

- **5m 是盘中训练的基础粒度**。在数据授权和预算允许的范围内，A 股和美股尽可能保存深历史 5m 数据。
- **1h 优先由 5m 按交易所时段聚合**，避免不同数据商对小时线切分规则不一致。
- **日线保存数据商提供的官方日线事实表**，目标是覆盖该品种可获得的完整历史。
- **周线由日线按交易所交易周聚合**，用于长期背景、回放和标记。
- 外汇/黄金进入第二批后沿用同一结构，但交易周、时区、tick volume 和周末开闭市规则单独配置。

#### 高周期的未来数据隔离

当用户回放 5m 或 1h 时切换到日线/周线，当前尚未结束的日 K/周 K 只能由“已经揭示的低周期 K 线”动态聚合。不能直接显示数据库中最终完成的当日最高价、最低价和收盘价，否则会泄露未来。

#### 历史覆盖目标

- A 股和美股：日线/周线尽量覆盖全部可获得历史；5m 覆盖深度由最终数据供应商、授权和成本确定。
- 外汇和黄金：第二批接入，优先覆盖主要货币对和 XAUUSD 的 5m、1h、日线和周线。
- 数据库必须显示每个品种/周期的最早时间、最晚时间、缺口数量、数据源和最近校验时间，不能让“有数据”和“数据完整”混为一谈。

#### 物理存储建议

- 行情按 `市场 / 周期 / 品种 / 年月` 分区保存为 Parquet，适合压缩和批量扫描。
- SQLite 保存最小品种目录、K 线覆盖目录、数据快照、训练记录和同步元数据。
- DuckDB 直接查询 Parquet，用于跨品种统计和复盘聚合。
- 不把全市场 5m 历史一次性塞进手机。Android 使用“离线数据包”：按市场、品种、周期和日期范围下载；云同步启用后，用户训练数据再保持完整同步。

#### 数据库管理中心

产品中需要有独立的“数据库”页面，而不是把维护工作藏在后台：

- 覆盖矩阵：按市场、品种、周期显示起止日期、K 线数量、缺口和数据源。
- 数据源管理：保存用户自己的 K 线 API Key、权限状态、限流信息和授权备注；密钥只放系统安全存储，不写入普通配置文件。
- 批量回填：选择市场、品种范围、周期和日期，创建可暂停/恢复的下载任务。
- 收盘后更新：按交易所时区执行增量更新，不需要实时订阅。
- 缺口修复：扫描缺失或异常 K 线区间，允许换数据源修补，但必须生成新版本，不能静默改写旧快照。
- 当前 A 股落地方案：TDX 官方完整包作为全历史基础层；初始化后用 Tushare 120 的 `daily(trade_date)` 按日期拉取整市场普通股票不复权日线。每日增量从有效最后日期继续，缺口修复默认回查最近 30 个自然日；节假日空响应和停牌未返回证券均不造 K 线。
- Tushare 增量写入独立 SQLite 覆盖层，只保存新增或发生修正的 K 线，读取时与 TDX 分区文件合并，周线再由合并后的日线聚合。不会复制现有 2500 多万根基础 K 线，也不会把仅有几天的增量错误地当成完整历史。
- 120 积分流程不依赖更高积分的交易日历接口，并按 1.4 秒请求间隔控制在 50 次/分钟以内；指数、基金和可转债仍由 TDX 完整包维护。
- 快照管理：查看哪些训练引用了某个数据版本；被引用的快照不可直接删除。
- 存储管理：查看各市场和周期占用空间，生成/删除 Android 离线数据包。
- 备份恢复：用户数据与行情数据分开备份；策略、标记和训练记录优先级高于可重新下载的行情缓存。

### 6.3 数据接入顺序

#### P0：先保证可用和可迁移

- CSV/Parquet 导入器作为第一公民，用户可以导入任何合法来源的 OHLCV。
- 内置字段映射、时区选择、重复 K 线/缺口/非法 OHLC 检查。
- 导入后生成不可变数据快照，训练不随数据源后续修订而改变。

#### P1：第一优先市场适配器

| 市场 | 建议首选 | 备注 |
|---|---|---|
| A 股 | Tushare Pro + CSV 兜底 | 官方文档覆盖日线、分钟线、交易日历、复权因子等；权限/积分和商用条款需单独确认 |
| 美股 | Alpaca 免费历史行情 + CSV 兜底 | 当前按用户自己的 API Key 下载；免费层限流和授权边界需持续遵守 |

#### P2：外汇和黄金适配器

| 市场 | 建议首选 | 备注 |
|---|---|---|
| 外汇 | Dukascopy CSV 历史基准 + Twelve Data REST 增量 | 不依赖 MT4/MT5 终端；历史与增量采用明确切换点，统一保留 M1，再由本地生成高周期 |
| 黄金 | Dukascopy/专业金属数据源 + CSV | XAUUSD 现货与交易所黄金期货分开定义；不直接复用外汇的交易时段和合约规则 |

AKShare 可作为个人开发期的便捷适配器，但它聚合上游站点，分钟历史范围和稳定性可能变化，不建议把它作为唯一生产数据源。

#### 外汇确定方案：Dukascopy 历史基准 + Twelve Data REST 增量

外汇采用“两段来源、一个逻辑数据集”的方案：Dukascopy 负责建立历史训练基准，Twelve Data 负责历史末端之后的增量更新。两者的报价聚合方式可能不同，因此不能让数据源在同一时间区间内静默混写，也不能在 Twelve Data 暂时失败时自动回退到 Dukascopy。

总体流程：

```text
网页数据页
  → 创建外汇初始化/更新任务
  → 服务端下载或请求数据
  → 临时文件分块解析
  → 统一 UTC 时间、品种代码和价格精度
  → OHLC 校验、去重、缺口检查
  → 保留 M1，并由 M1 聚合 5m/1h/1d/1w
  → 写入行情库和覆盖目录
  → 生成质量报告与不可变训练快照
```

##### 1. Dukascopy 历史初始化

- 数据页新增“外汇 → 初始化历史数据”入口，用户选择货币对、起止日期和是否保留原始文件。
- 优先调用 Dukascopy 官方历史数据导出服务获取 CSV；下载器封装成独立的 `DukascopyHistoricalClient`，网页和数据库层不直接依赖易变的下载 URL。若官方导出服务发生变化，只替换连接器，不改导入和训练层。
- 初始下载使用 M1 作为原始粒度。服务端采用流式下载、临时文件和分块解析，不把完整文件一次性读入浏览器或内存。
- M1 数据先聚合成项目的 5m 基准，再由 5m 聚合 1h；日线和周线也由同一套聚合规则生成，避免不同数据商对小时线、日线切分不一致。
- 原始时间戳一律转换为 UTC 毫秒；第一版固定记录 `America/New_York` 的 17:00 作为外汇交易日切分配置，配置项必须可调整，但同一品种历史和增量不得混用不同切分规则。
- 默认过滤周末无报价区间；正常周末闭市不算缺口，临时中断、异常长空洞和重复时间戳必须进入质量报告。
- 内置适配器优先使用稳定的 Jetta 正式服务器；单日请求遇到网络断开、超时、HTTP 429 或 5xx 时最多尝试 3 次并指数退避，遵守 `Retry-After`。下载器支持最多 4 个交易日受控并发并跨分片复用品种元数据；当前长历史任务按 7 天分片调用并保存游标，兼顾吞吐、失败重试范围和页面响应。widget 指向测试节点时不再回退到本机不可达的测试主机。

推荐的内部品种映射示例：

| 内部品种 | 显示名称 | Dukascopy/Twelve Data 映射 | 精度 |
|---|---|---|---:|
| `EURUSD.FX` | EUR/USD | `EURUSD` / `EUR/USD` | 5 |
| `GBPUSD.FX` | GBP/USD | `GBPUSD` / `GBP/USD` | 5 |
| `USDJPY.FX` | USD/JPY | `USDJPY` / `USD/JPY` | 3 |
| `AUDUSD.FX` | AUD/USD | `AUDUSD` / `AUD/USD` | 5 |
| `USDCAD.FX` | USD/CAD | `USDCAD` / `USD/CAD` | 5 |
| `USDCHF.FX` | USD/CHF | `USDCHF` / `USD/CHF` | 5 |

外汇的 `volume` 默认为空；如果供应商返回的是 tick volume，必须明确标记为 tick volume，不能在训练筛选器中当作股票成交量或集中市场成交量。`turnover` 默认为空。

##### 2. Twelve Data 增量更新

- Twelve Data API Key 通过现有“设置 → 数据源设置”保存在本机凭证表或环境变量中，只由服务端读取，不能下发到浏览器。
- 增量任务沿用现有可暂停、恢复、重试的下载任务模型；新增 `twelvedata` provider，并限制为 `market = FX`。
- 增量连接器只请求 Twelve Data `/time_series` 的 `1min`，写入已经收盘的 M1，再由本地聚合 5m/1h/1d/1w；不同时请求供应商 5m，避免两套周期形成 OHLC 差异。
- 跨来源边界使用 Dukascopy 最后一根 M1，增量检查点使用 Twelve Data 最后一根 M1。旧数据库如果只有 Dukascopy 5m，则从该 5m 桶结束后的第一分钟开始，不会把已有 5m 误判为已经存在 M1。
- 每次更新从该品种最后一根“已完成”5m K 线之后开始拉取。可以额外读取少量重叠 K 线用于质量比较，但历史切换点之前的 Dukascopy K 线不被 Twelve Data 静默覆盖。
- 丢弃当前尚未收盘的 K 线；任务结束时只把完整 K 线写入行情库，并记录最后成功时间、最后完成 K 线时间和供应商游标。
- Twelve Data 请求失败时保留任务为失败或待重试状态，不自动切换回 Dukascopy；否则会把不同报价源拼成一段看似连续但不可复现的行情。

##### 3. 历史与增量切换规则

- 每个品种/周期记录 `historyBoundary`，即 Dukascopy 历史数据的最后一根完整 K 线。
- 切换前保留一个只读校验窗口，比较两套来源在边界附近的若干根 K 线，并按品种精度和可配置的点差容忍度生成报告。
- 校验差异只报警，不自动修补 Dukascopy 历史；从 `historyBoundary + timeframe` 起由 Twelve Data 独立负责后续数据。
- 同一来源内部允许重叠更新，用于修正最近已收盘 K 线；跨来源不允许无记录地覆盖。行情表的 `source`、任务记录和覆盖目录必须保留来源信息。
- 训练快照绑定数据内容哈希。后续 Twelve Data 更新只生成新快照，已经引用旧快照的训练不能被行情更新改写。

##### 4. 网页任务和存储设计

沿用现有数据库任务、覆盖目录和快照，不新增独立的外汇训练内核。数据页需要增加：

- “初始化历史数据”：选择货币对、日期、原始粒度和是否保留 CSV。
- “更新外汇数据”：按品种执行 Twelve Data 增量，可查看最后成功时间和待补区间。
- 阶段进度：下载、解析、聚合、校验、入库分别显示进度。
- 任务操作：暂停、恢复、重试、取消，以及失败区间和错误原因。
- 覆盖矩阵：显示最早/最晚时间、K 线数量、缺口、来源、历史切换点和最近校验时间。
- 原始文件暂存于本地行情数据目录；导入完成后可按任务设置删除，以免 M1 原始文件长期占用空间。

当前 CSV API 仍以 5000 根 K 线为单次上限；网页端已按 4000 根分片提交，FX CSV 会先解析并聚合为 5m。后台历史任务已按日期分片续跑，并支持 M1/5m/1h/1d/1w 的明确周期参数。普通 CSV 手动导入继续保留，作为官方下载失败或用户已有数据时的兜底入口。

##### 5. 外汇专用质量和合规要求

- 校验 `low <= min(open, close) <= max(open, close) <= high`、时间戳唯一、周期对齐、价格精度、重复和异常跳变。
- 缺口扫描使用外汇交易周配置，不能把周末和正常报价暂停误报成缺失；周一开盘和周五收盘附近单独显示。
- 交易模拟必须另行配置点差、滑点、杠杆、合约大小、隔夜费和 bid/ask 方向；仅有单一 OHLC 中间价时，训练报告必须标注“未包含实时点差”。
- 外汇是 OTC 市场，没有一份所有经纪商共同认可的集中成交量；不同来源的 K 线不能直接当作完全相同的事实序列。
- 个人本地使用和向训练营学员展示/再分发属于不同授权场景。接入 Twelve Data 或其他供应商前，必须确认缓存、展示、商用和再分发条款。

##### 6. 分阶段实施

1. **FX-1：数据模型与品种目录**：已完成 FX provider、凭证状态、六个首批货币对映射、UTC/交易日切分和价格精度。
2. **FX-2：Dukascopy 下载任务**：已完成官方内置适配器、Jetta 配置解析、正式服务器优先、官方 `EUR-USD/BID` 分钟路径、单日自动重试、指数退避、受控并发下载、超时诊断、CSV 标准化、M1 持久化、M1→5m→高周期聚合、质量报告和持久化任务；训练页支持 1m 回放，仍保留可选自定义 CSV 端点覆盖。
3. **FX-3：Twelve Data 适配器**：已完成 M1 REST 拉取、已收盘分钟线过滤、M1 入库、本地高周期聚合、分钟游标、限流错误识别、失败恢复和历史边界保护。
4. **FX-4：数据页操作面板**：已完成历史初始化、增量更新、阶段进度、暂停/恢复/重试/取消和质量摘要；覆盖矩阵和 FX 专用交易规则仍属于后续阶段。
5. **FX-5：训练和成交规则**：`2026.08-v4` 已按 BID K 线与冻结点差生成 Ask，买入/回补使用 Ask、卖出/平多使用 Bid，限价/停止单和保护单也按对应报价侧触发；已接入 0.01 手、10 万基础币合约、Pip/Point 价值、基础币/报价币自动换算、第三币种固定汇率、杠杆、保证金、可用保证金、保证金水平和逐 K 强平。仍需补交易周、隔夜费、动态/真实 Ask 序列和跨来源回归测试。

### 6.4 复权原则

- 数据源提供未复权、前复权或后复权序列时，将它们视为同一品种的不同 K 线数据集，分别保存 `adjustmentType`，不在本地重算。
- 图表必须明显显示当前复权类型，训练创建后锁定，不允许中途切换。
- 模拟成交优先使用未复权 K 线；使用复权 K 线时，报告标注“训练近似结果”。
- 当前不建设公司行动明细库，因此不尝试在本地重建拆股、分红或送转过程。

### 6.5 数据质量检查

- `low <= min(open, close) <= max(open, close) <= high`。
- 时间戳唯一且递增。
- 按最小交易时段配置检测缺口，不把 A 股午休或正常闭市误判成缺失。
- 检测异常跳空、零价格、负成交量、重复记录。
- 训练报告显示数据源和质量警告。
- 每次增量更新后执行去重、断档和 OHLC 一致性检查；异常数据进入隔离区，不直接覆盖已用于训练的数据快照。

### 6.6 数据合规

即使不做实时行情，也要区分“个人下载使用”和“向其他用户重新分发行情”。若未来做成 SaaS，必须重新确认每个市场和数据商的展示、缓存、再分发和商用授权。第一版采用本地导入和用户自己的 API Key，能显著降低风险，但不能替代正式授权审查。

## 7. 技术架构建议

### 7.1 第一版：本地优先、移动端就绪的 Web 应用

| 层 | 建议 |
|---|---|
| 前端 | React + TypeScript；响应式布局和触摸交互从第一版纳入验收 |
| K 线组件 | 优先评估 KLineChart 10.x；自带 Overlay 体系，适合画线和语义标记 |
| 本地后端 | Python + FastAPI |
| 数据处理 | Polars/Pandas；Parquet 保存行情 |
| 数据库 | Parquet 行情仓库 + SQLite 元数据/训练记录 + DuckDB 分析查询 |
| AI 总结 | 确定性指标引擎 + 可替换的 LLM Provider |
| 导出 | Markdown、JSON、CSV、PNG |

选择 KLineChart 的原因：Apache 2.0、对 K 线场景专注、内置/自定义 Overlay 丰富，官方数据接口也明确支持本地数组逐根推送，适合 Replay。TradingView Lightweight Charts 也可作为备选，它同样是 Apache 2.0，但绘图和语义标记需要更多自建工作。

> 注意：KLineChart 10.x 在 2026 年仍有较新的重大版本变化。开工前先做一个最小技术验证，并锁定具体版本，避免中途追 beta。

#### Android 路线

- 第一版先完成响应式 Web/PWA 体验，确保手机浏览器能完成查看、逐根推进、下单和轻量标记。
- Android App 阶段优先复用 React 前端和同一套领域模型，通过移动端外壳封装；只有图表性能、手势或离线能力不达标时，再把局部模块改为原生实现。
- 回放引擎、成交规则、指标计算和策略模型独立于 UI；桌面端与 Android 端使用相同测试用例。
- 手机端默认使用选择性离线数据包，不承担全市场完整历史库。

### 7.2 云同步预留，但暂不开发

#### 将来需要同步的内容

- 策略、策略版本和模板。
- 训练会话、事件日志、订单和统计结果。
- 图表标记、笔记、错题和 AI 结论状态。
- 用户偏好和离线数据包目录。

#### 默认不跨设备同步的内容

- 全量行情数据库。各设备根据 `snapshotId/contentHash` 下载或重建所需数据包，避免云端反复传输海量 K 线。
- 可重新生成的截图和缓存；只同步必要的用户资产。

#### 现在就写入数据模型的同步字段

```ts
type SyncableEntity = {
  id: string             // UUID，不使用本地自增 ID
  version: number
  createdAt: string
  updatedAt: string
  deletedAt?: string     // 墓碑删除，避免离线设备把已删除数据恢复
  deviceId: string
  syncState: 'local' | 'pending' | 'synced' | 'conflict'
}
```

第一版不需要账号系统和云服务，但这些字段可以避免未来迁移时重做全部数据库。

### 7.3 关键边界

```text
Data Adapter → Normalize/Validate → Immutable Snapshot → Replay Engine
                                                       ↓
Chart UI ← Session Event Log ← Execution Engine ← User Decision
                       ↓
        Metrics Engine → Review Features → AI Summary
                                      ↓
                               Strategy Candidate
```

- `Replay Engine` 只负责当前可见游标和逐根推进。
- `Execution Engine` 只负责订单状态和成交，不读取未来 K 线。
- `Session Event Log` 记录每次决策、标记、下单、修改和推进，属于审计真相。
- `Metrics Engine` 可以完全重放事件日志，重复得到相同报告。
- `AI Summary` 只能读取已完成训练的结构化特征，不参与成交计算。

### 7.4 事件日志示例

```json
{
  "sessionId": "S-20260719-001",
  "visibleBarIndex": 8421,
  "eventType": "TRADE_PLAN_SUBMITTED",
  "createdAt": "2026-07-19T09:42:13Z",
  "payload": {
    "side": "long",
    "marketState": "trading-range",
    "location": "range-low",
    "reasons": ["failed-breakout", "second-entry"],
    "stop": 12.34,
    "target": 12.88,
    "confidence": "medium"
  }
}
```

## 8. AI 总结如何避免“看起来聪明但没有用”

### 输入

- 策略版本与规则。
- 每笔交易的事前决策卡。
- 系统计算的 R、MFE/MAE、持仓时长和流程检查结果。
- 用户标记的市场状态/位置/理由。
- 可选：关键 K 线窗口的结构化 OHLC，而不是默认上传整个历史库。

### 输出

- 三个最可靠的发现。
- 两个高频错误和原始样本链接。
- 一个候选规则修改。
- 一个反例：说明新规则可能在哪种市场中失效。
- 一个下一轮训练任务。

### 约束

- 少于约 20 笔相关样本时，只能称为“线索”，不能称为“优势”。阈值后续可配置。
- 强制区分训练内样本和验证样本。
- 不使用只靠胜率得出的结论。
- 每个结论都能跳回对应 K 线和交易。
- 用户可以将 AI 结论标记为接受、拒绝、待验证；拒绝原因也会保存。

## 9. MVP 范围建议

### 必做

- CSV/Parquet 导入和统一 Candle 模型。
- A 股和美股各打通一个可持续更新的数据适配器；外汇/黄金保持同一适配器接口并进入紧随其后的版本。
- 建立详细 K 线库、最小品种目录、覆盖/质量目录和不可变数据快照。
- 支持 5m、1h、日线和周线；1h 从 5m 聚合，周线从日线聚合。
- 低周期回放切换到日线/周线时，动态生成未完成高周期 K 线，不能泄露最终值。
- 单图回放：选起点、随机起点、逐根、播放、倍速。
- 画线、标签、文字标记。
- 市价/限价/止损单，下一根开盘成交规则。
- A 股与美股两套市场规则配置；外汇/黄金规则接口预留。
- 事前决策卡和完整事件日志。
- 训练历史与完全复现。
- 确定性报告 + 第一版 AI 总结。
- 策略版本和候选规则。
- 桌面响应式界面通过常见手机尺寸的触摸操作测试。
- 所有用户对象采用 UUID、版本号和软删除，为以后云同步预留。

### 暂不做

- 实时行情和实盘下单。
- Tick 级回放、盘口、逐笔成交。
- 财务、基本面、新闻、公告、宏观、公司行动明细和其他非 K 线数据库。
- 多图联动和多周期同步回放。
- 账号、真正的云同步和云端全量行情分发。
- 原生 Android App；第一版只保证响应式 Web/PWA 路线可行。
- 复杂组合、多账户、期权 Greeks。
- 社区、排行榜、跟单。
- 自动寻找“稳赚策略”。

### MVP 验收标准

- 同一数据快照和随机种子可以 100% 恢复同一次训练。
- 回放过程中无法从接口或 UI 读取未来 K 线。
- 下一根开盘成交、手续费和 OHLC 冲突规则有自动化测试。
- A 股 T+1、整手和涨跌停至少有基础测试。
- 美股常规交易时段和可选做空规则至少有基础测试；拆股/分红明细暂不处理。
- 5m → 1h、日线 → 周线聚合遵守交易所时区与日历；回放中的未完成高周期 K 不含未来数据。
- 标记恢复后仍锚定原 K 线和价格。
- 数据库能够报告每个品种/周期的覆盖范围、缺口、来源和质量状态。
- 报告中的每个统计可追溯到具体交易。
- AI 的每个主要结论包含样本量和至少一个原始案例链接。

## 10. 建议开发顺序

### 第 0 阶段：产品原型

- 先用四份固定数据：A 股和美股各一份 5m/日线数据，并准备一个外汇与一个黄金样本验证数据模型兼容性。
- 验证回放控制条、决策卡、标记、模拟成交和报告页面的交互。
- 验证 5m/1h/日线/周线切换、高周期动态聚合和手机触摸操作。
- 不急着接很多 API，也不做云同步。

### 第 1 阶段：可用 MVP

- 完成 A 股/美股 K 线导入、覆盖/质量目录、快照、事件日志和两套市场规则。
- 打通训练创建 → 回放 → 模拟交易 → 复盘 → 重开历史训练。
- 先使用规则模板生成 Markdown 总结。

### 第 2 阶段：策略优化

- 增加 AI 归纳、策略版本、样本外验证、错题重练。
- 按标签和市场状态做聚合分析。

### 第 3 阶段：扩展

- 外汇按“Dukascopy CSV 历史基准 + Twelve Data REST 增量”完成数据闭环；再补黄金完整适配器、多周期上下文和专项题库。
- 完成响应式/PWA 验证后发布 Android App；再根据实际需要实现可选云同步。

## 11. 我给出的第一版默认方案

如果现在就开始做，我建议先采用：

- **形态**：本地优先的桌面 Web 应用，未来可迁移到云端。
- **优先市场**：A 股 + 美股；外汇/黄金在下一批紧跟，不另起一套系统。
- **优先周期**：原定为 5m、1h、1d、1w；当前外汇链路已经保留 M1 并开放 1m 回放，其他市场仍按各自已入库覆盖决定可选周期。
- **外汇数据**：Dukascopy M1 CSV 作为历史基准；历史末端之后使用 Twelve Data REST 拉取已收盘 M1，两段都由服务端统一聚合成 5m/1h/1d/1w。
- **外汇更新**：网页创建可暂停/恢复/重试的后台任务；API Key 只由服务端读取；历史切换点、来源、质量报告和训练快照必须可追溯。
- **图表**：KLineChart 10.x 做技术验证后锁版本。
- **数据**：只建设详细 K 线库；使用 Parquet K 线仓库 + SQLite 最小目录/训练记录 + DuckDB 分析；CSV/Parquet 导入永远保留。
- **交易**：先做净持仓、单账户、基础订单、固定/百分比风险仓位。
- **复盘**：先做确定性指标和流程评分，再接 AI 生成解释和候选规则。
- **云同步**：暂缓开发，但所有用户对象按可同步结构存储；行情库不做全量同步。
- **Android**：第一版即做响应式和触摸适配；后期用选择性离线数据包发布 Android App。
- **训练哲学**：每根 K 先判断结构、位置和交易条件；没有优势时允许并奖励 `no-trade`。

## 12. 需要我们下一轮确认的问题

1. A 股模拟是否必须从第一版就严格支持 T+1、涨跌停和停牌？公司行动明细已确定暂缓。
2. 美股是否需要盘前/盘后和做空？本方案默认第一版只做常规交易时段，做空可配置。
3. 对详细 K 线库，你更看重全市场完整日线，还是更深的全市场 5m？两者的数据授权、体量和成本差异很大。
4. Tushare 与 Alpaca 凭证由用户在“设置 → 数据源设置”保存到本机数据库；`.env.local` 仅作高级兼容方式。后续如需全市场批量库，再确认数据权限、容量与更新频率。
5. 你的策略是否偏 Al Brooks/纯价格行为？如果是，决策卡和标签可以直接围绕趋势、震荡、突破、回调、反转、第二次入场等设计。
6. AI 总结可以调用云模型吗，还是交易记录必须完全留在本地？
7. Android 端是否需要在没有网络时完成完整训练？本方案默认支持已下载数据包的离线训练。

## 13. 实现进度与差距盘点

### 13.1 2026-08-11：当前本地原型

当前项目已切换为纯本地运行，不包含 Sites 托管、账号或云同步配置。双击 `启动本地网页版.bat` 即可启动；本地行情和训练数据暂由项目内的 Miniflare/D1 开发数据库保存。

#### 已完成

- 本地 Web 应用、中文界面、桌面优先的响应式布局和一键启动 BAT。
- 启动 BAT 会先识别并复用已运行的本项目数据服务；新启动 3100 服务时最多等待 30 秒完成大型目录加载，并将“服务尚未就绪”和“端口确实被其他程序占用”分别提示，避免初始化后误报端口冲突。
- A 股贵州茅台、美股 Apple 内置样例行情；统一周期入口支持 1m、5m、1h、1d、1w，具体可选范围按品种已有覆盖决定。外汇历史任务会同时保存 M1 并生成 5m、1h、1d、1w。
- 隐藏未来、随机起点、上一根/下一根、前进 5 根、播放/暂停、0.5x/1x/2x/5x。
- 新建训练可选择指定日期、指定第 N 根 K 线、随机或默认起点；可设置揭示长度，到达边界后自动结束。
- 基本设置可配置训练起点左侧显示的历史窗口，范围为 100～5000 根；该值在新训练创建时写入任务，已保存训练不会因以后修改设置而改变。
- 自由训练、盲测、固定测试区间和错题重练均已落地；错题当前由低于 80% 的正式计划和市场规则拒单生成。
- 品种名称、日期坐标和绝对价格可独立隐藏；盲测默认全部隐藏，完成前锁定复盘入口，从明确保存点恢复后仍保持隔离。
- 训练过程不再自动进入“可恢复训练”；只有手动保存或到达训练终点才建立保存点。
- 顶部“随机训练”与“新建 Replay 训练”已拆分为两个独立入口；随机训练可选择普通模式或隐藏答案的盲测模式。
- 随机训练到达终点后弹出本局结果，可继续随机、查看复盘，或通过 X 退出随机训练循环。
- “继续随机”会使用完成局锁定的随机配置快照，完整继承品种池、周期范围、历史日期、训练长度、盲测隐藏项、指数范围和形态筛选，不受本局随机结果改变当前品种/周期的影响。
- 侧栏设置已分为基本设置和训练设置；独立随机训练入口可配置当前/全部/指定市场品种池、当前/全部/指定周期、历史时间段和默认长度。市场专用限制按随机范围条件显示：A 股范围才显示指数选择，美股范围才显示流动性过滤，指定 FX 等其他市场时不显示无关规则。随机池默认仅保留当前可交易品种，可选择纳入指数；指数局明确标记为只看盘训练并关闭模拟买卖。
- 独立“表现”板块默认分析全部已保存训练的累计盈亏、训练场次、交易胜率、平均每场、Profit Factor 和最大回撤；筛选器置顶，避免默认状态重复显示两套相同指标。
- 表现页可按品种、周期、训练模式、形态预设、完成状态及保存日期筛选训练集，并重新计算组合盈亏、胜率、回撤、计划完整度和胜负分布；同时按已平仓交易分析持仓时长、市场状态、位置、理由、计划填写习惯、实际命中形态和完整习惯组合，至少两笔样本才标记为当前最优。严格事前计划优先；旧版补写计划在没有严格事前记录时作为兼容样本，并在界面分别显示归因数量。复盘展示所选形态及本局实际命中形态。
- 训练明细的“查看复盘 / 继续训练”已放入每行最右侧，桌面端悬停或键盘聚焦时显示，手机端保持常显，无需先选中再滚动到列表底部。
- 基本/训练设置、形态预设、随机方式与随机形态、MA/EMA、自定义理由标签及绘图偏好统一保存到本机 D1；电脑和手机访问同一个本地服务时可跨浏览器来源同步，重新打开页面、窗口重新聚焦或手机回到前台时刷新设置。
- 筛选结果支持选中单次训练，直接查看结构化复盘或恢复训练。
- 水平线、趋势线、通道、斐波那契、自由画笔和简单 K 线标记入口。
- 绘图工具已补充矩形、箭头、文字标记、磁吸、样式修改、锁定、显隐、删除、撤销/重做和独立对象树；对象可随训练保存和恢复。
- 成交逻辑已抽离为纯函数并升级到 `2026.08-v4`：市价委托遵循“第 N 根收盘提交、第 N+1 根开盘成交”；限价单和止损触发单按 OHLC 触发并可跨 K 线等待，跳空时按开盘价与触发价的有利/不利边界成交；外汇按 BID/Ask 报价侧触发并使用保证金账户。
- 成交参数新增单根 K 线成交量参与上限。触发数量超过当根可成交量时只生成部分成交，订单累计已成交数量、按比例释放现金预留，未成交余量保持同一委托继续等待；保护退出和普通平仓也可生成关闭切片并保留剩余受保护仓位。`0%` 表示兼容旧训练的整单成交模型。
- 下单支持固定数量和账户权益风险百分比。百分比模式要求先设置方向正确的止损，按入场与止损的不利执行价、点差、滑点和双边佣金估算单位风险，再按市场最小数量/增量和可用现金向下取整；训练创建时冻结默认模式和风险比例。
- 开仓可附带自动止损和止盈；入场当根不回看触发保护单，从下一根开始生效。同一根 K 线同时命中止损与止盈时，可选择保守先止损、乐观先止盈或基于仓位与时间戳的种子确定路径，冲突事实写入仓位和成交记录。
- 基本设置可配置佣金 bp、最低佣金、滑点 bp、买卖价差 bp 与同根冲突规则；新训练创建时锁定这些参数，保存/恢复继续使用原参数。成交记录同时保留原始触发价、成本调整后价格、费用、价格影响、成交原因与引擎版本。
- “前进五根”和倍速播放会逐根运行成交引擎，不会跳过中间 K 线中的限价、止损触发或保护单；训练结束自动平仓同样使用冻结的成本模型。
- A 股主板、科创板、创业板与北交所普通股票已接入版本化基础市场规则：分别处理最低买入数量、买入增量、T+1、10%/20%/30% 涨跌幅和 IPO 初期无涨跌幅天数；下单、成交、拒单和恢复训练均绑定规则版本。指数保持只看盘模式。
- 支持同时持有多笔独立多头/空头仓位、逐笔平仓、全部平仓、待成交撤单和已平仓记录。
- 模拟账户支持“收益率模式”和“资金账户模式”；资金账户可配置初始资金，按现金、待成交买单预留、持仓市值和账户权益核算，资金不足时拒单。
- 图表显示买入、卖出、平仓点及开平仓连接虚线；默认半透明，悬停后完整显示。
- 止损和止盈输入已改为点击图表取价，所选价格同步显示在下单框和事前决策卡，并在图表形成保护线。持仓建立后可直接拖动保护线；多头止损只能上移且低于当前价，空头止损只能下移且高于当前价，拒绝放宽风险。初始风险保持冻结，移动后的实际出场继续进入成交与复盘证据。
- 事前决策卡：市场状态、位置、理由、止损、目标和计划说明；点击提交后冻结完整内容并绑定当时K线。若用户只在图表设置止损/止盈，系统会立即生成一张绑定当前 K 线的保护计划卡；同一根 K 线继续选择另一保护价时更新同一张自动卡，之后正式提交完整计划会替换自动卡，避免重复记录和保护价脱节。
- 本地保存训练会话、确定性复盘指标和训练历史列表；启动时恢复最近一个明确保存点，未手动保存且未完成的临时进度不会进入历史。
- 训练快照覆盖回放位置、订单、持仓、成交、决策卡、下单数量、随机种子和用户绘图；手动保存或训练结束时同时写入浏览器保存点和本地 D1。
- 每次新训练绑定 SHA-256 内容哈希的不可变 K 线快照。当前快照格式为 v2：K 线按周期分桶（1m/日、5m/周、1h/月、1d 与 1w/年），再按不超过约 1.25 MB 拆成紧凑 JSON 块；块以内容哈希寻址，相同内容可被多个快照复用，快照只有全部分块校验通过后才进入 `ready` 状态。
- 快照 API 支持按起止时间和左侧回看根数读取窗口；恢复训练会按原任务区间读取所需分块，并用全局索引偏移还原游标、决策和绘图位置，表现页的成交前 20 日流动性分析也只取需要的历史窗口。旧 v1 完整/差异链快照继续兼容读取，但新建快照不再追加差异链。
- 推进、回退、播放、数量、下单、撤单、成交、正式决策提交、绘图、保存和恢复均写入独立有序事件日志；决策草稿只保留在当前内存，直到手动保存或训练完成，不按字符生成审计噪声。
- 已提交决策在对应K线上显示“计划”标记，点击可查看市场状态、位置、理由、止损、目标和说明。
- 复盘页完整显示历次决策卡与可展开操作时间线；历史训练可以直接“查看复盘”、继续训练或二次确认删除。
- 单场确定性复盘指标已完成首版：逐笔毛/净盈亏与费用、冻结初始风险、R、MFE/MAE、MFE-R/MAE-R、出场效率、入场后 1/3/5/10 根路径、期望值、平均盈亏、盈亏比、Profit Factor、逐 K 盯市权益最大回撤/回撤比例/水下时长和最大连续亏损。逐笔记录保存入场、出场、MFE、MAE 时间戳与证据完整状态；没有初始止损的旧交易将 R 明确显示为不可用，不用估算值替代。
- K 线数据库覆盖页、CSV OHLCV 导入、基础 OHLC 合法性校验和样例周期聚合。
- 数据库页按 A股、美股、外汇、黄金拆分为独立管理视图，各市场分别统计品种、K线、周期、任务和覆盖明细，不再混排。
- 单标的 Tushare/Alpaca 输入表单已移除：A股以通达信全市场包初始化，之后用 Tushare 120 按交易日做全市场普通股票日线增量和最近 30 日缺口回查，必要时仍可刷新 TDX 完整基础包；美股通过 Alpaca 活跃可交易证券目录批量初始化 SIP 日线，后续从各品种最后日期继续更新。SIP 权限失败会明确暴露，不静默回退 IEX。
- 美股市场级任务按 Alpaca 交易日历、数据点预算和 URL 长度计算多品种批次，并以全局请求间隔限速；任务可暂停、继续、取消，失败批次可拆分定位坏品种后重试。
- Alpaca 证券目录同时兼容官方资产响应中的 `class` 字段和旧适配器使用的 `asset_class` 字段，避免有效目录在初始化时被错误过滤为空。
- 侧栏设置新增“数据源设置”，可保存、替换或清除 Alpaca/Tushare 个人凭证；凭证只落在本机数据库，接口仅返回配置状态和掩码，不回显完整密钥，`.env.local` 保留为兼容备用。
- 数据页首次进入新增“快速初始化 / 高级自定义”双入口：快速方案固定为沪深京 A 股全历史日线、退市样本、交易所指数和自动周线；高级方案可选择市场、品种、历史范围、最小周期，并为首次日线、增量、分钟和美股分别安排通达信 ZIP、Tushare、TdxQuant、Alpaca 或本地文件。
- 快速初始化已连接本机数据服务：从通达信官方地址断点下载 `hsjday.zip`，校验 ZIP 和 SHA-256 数据版本，按沪/深/北交易所扫描 `.day`，自动区分股票、指数、基金和可转债，并保存可暂停、恢复、失败重试的任务状态。
- 完整包导入后自动从在线证券目录补全中文名称，也可在任务卡手动更新；无法匹配的少量历史代码继续显示确定性占位名，避免错误命名。
- 通达信日线采用分区文件仓库，不再把全市场数千万根 K 线重复写进 D1；统一 `/api/candles` 会合并本机分区目录和原有 D1 数据，Replay、随机训练、覆盖统计与不可变快照无需改入口即可读取。周线由日线按需聚合。
- A 股初始化后的维护已接入 Tushare 120：按工作日调用 `daily(trade_date)` 一次读取全市场，支持每日增量、最近 30 日缺口/修正回查、任务暂停和跨服务重启恢复；Token 只在启动/恢复时由本机设置转交，不写入任务文件。新增与修正进入轻量 SQLite 覆盖层，Replay 读取时与 TDX 全历史合并。
- 数据覆盖表支持逐行勾选、全选当前页和二次确认删除；样例、CSV 与下载数据可按具体覆盖记录删除，TDX 日线/周线按品种联动删除，既有不可变训练快照不受影响。
- 数据源设置可保存 TdxQuant 本机 HTTP 端点，并限制为 `127.0.0.1` 或 `localhost`；当前已用于初始化方案和连接状态展示，实际分钟批量读取连接器仍待接入。
- 数据库页面只负责真实行情下载和任务管理；下载完成的新标的自动进入 Replay、训练设置和随机训练品种池。
- 外汇数据页已完成 Dukascopy 历史初始化、Twelve Data 已收盘 M1 增量、任务暂停/恢复/重试/取消、分片心跳和质量摘要；训练页可选择已入库的六个首批货币对及 1m/5m/1h/1d/1w 周期。
- 顶部“实盘筛选”已接入 A 股和美股最近完整收盘日筛选；筛选结果可进入独立实盘观察账本或观望列表，实盘表现与训练表现隔离统计。这里仍是本地观察与模拟成交，不连接券商实盘下单。
- MA/EMA 指标已接入桌面和手机工具栏，可独立开关、配置周期并只使用已经揭示的数据计算。
- 生产构建、ESLint 和 130 项自动测试当前全部通过。成交引擎测试覆盖市价/限价/止损触发、跳空价格、手续费/点差/滑点、同根止盈止损冲突、保护单生效边界、市场规则拦截、现金与净盈亏、不触发委托保留、拒单消费、部分开仓、剩余委托和部分平仓；风险仓位测试覆盖成本预算、数量增量、现金上限和错误方向止损。复盘指标测试覆盖 R、MFE/MAE、后续路径、期望值、盈亏比、Profit Factor、逐 K 回撤、连亏和证据缺失标记；快照测试覆盖确定性分桶、大小上限、内容复用、时间窗/回看读取、全局索引和旧 v1 兼容。图表点击取价、决策联动和 FX/全市场/美股规则显隐已完成真实浏览器验收。

### 13.2 已部分实现，但尚未达到方案要求

| 模块 | 当前状态 | 仍缺少 |
|---|---|---|
| 本地数据库 | 已有品种、K 线、训练会话和覆盖信息；通达信全市场日线使用按交易所/代码分区的原生 `.day` 文件仓库；不可变训练快照已使用内容寻址分块、分块映射和范围索引 | D1 仍承担训练与小规模导入；未采用 Parquet + SQLite + DuckDB，未做列式分析缓存、快照引用可视化、未引用分块回收和存储空间管理；新快照创建仍会先读取完整序列，尚未做到全链路流式构建 |
| 数据导入 | 支持 CSV、通达信 A股全市场包、Alpaca 美股目录批量初始化与市场级更新、Dukascopy/Twelve Data 外汇任务，提供基础字段检查、去重、质量计数和可恢复任务，训练时自动冻结不可变快照 | 未支持 Parquet、通用字段映射、股票市场交易日历缺口扫描、异常隔离和完整导入报告；外部数据源仍需用户自己的账户凭证和相应权限 |
| 周期 | 统一入口支持 1m、5m、1h、1d、1w；FX 可由 M1 生成全部高周期，TDX 日线可按自然周生成周线 | 股票盘中周期尚未按交易所日历统一聚合；低周期回放切换高周期时，尚未动态生成“未完成日/周 K”以彻底隔离未来 |
| 训练任务 | 支持日期/第 N 根/随机起点、指定长度自动结束、自由训练、盲测、测试区间、错题重练和三类隐藏项；任务边界及完成状态可恢复 | 错题标准仍较基础；未做题库标签、难度、间隔重复和多错题队列 |
| 训练保存 | 手动/结束保存、历史恢复/删除、任务信息与保存时盈亏、不可变 `data_snapshot_id`、内容哈希、随机种子、市场规则快照、`2026.08-v4` 成交引擎版本、成交参数、仓位模式/风险比例、外汇合约与保证金规则、确定性指标快照和独立事件日志均已落库；v2 快照支持按任务区间和历史窗口恢复，并兼容 v1 快照 | 仍缺从事件日志一键重算全部状态的独立 Event Replayer、跨引擎版本迁移/对比；升级前且尚未绑定快照的旧训练，无法找回过去已被覆盖的行情 |
| 图表标记 | 绘图工具、交易标记、已提交决策标记和用户 Overlay 均可保存恢复；已有独立对象树及显隐、锁定、删除、撤销/重做 | 还没有通用语义标签、创建时可见边界、来源字段和“仅本局/跨训练/策略模板”可见范围 |
| 模拟交易 | `2026.08-v4` 纯函数引擎支持市价/限价/止损触发单、跳空、自动止盈止损、佣金/最低佣金/点差/滑点、三种同根冲突策略、按成交量参与率部分成交、剩余订单、部分关闭切片、固定数量/权益风险百分比仓位、图表保护价和移动止损；资金账户校验现金购买力；FX 已完成 Bid/Ask 方向、手数/合约/点值/币种换算、杠杆/保证金/逐 K 强平；A 股多板块普通股票已有基础边界 | 尚未做固定金额风险、用户指定分批退出/分批止盈；A 股特殊证券与美股专用时段、FX 交易周/隔夜费、黄金专用合约和费用规则仍未完成 |
| 决策卡 | 有结构、位置、理由、止损、目标、说明；提交后冻结并绑定K线，可在盘面、当前复盘和历史记录中查看；说明文本可以记录等待和放弃条件 | 缺少独立结构化的主导方、方向、触发价、放弃条件、置信度、`no-trade`、分批/移动逻辑和策略版本 |
| 复盘与表现 | 单场复盘已保存逐笔净/毛盈亏、费用、R、MFE/MAE、出场效率、后续 1/3/5/10 根路径、期望值、盈亏比、Profit Factor、逐 K 权益回撤/水下时长、连亏和证据时间戳；整体表现支持训练集筛选、盈亏/胜率/PF/训练序列回撤、计划完整度、持仓/决策/形态/组合习惯分析 | 新确定性指标目前以单场保存快照和明细展示为主；仍缺点击证据跳回图表、跨训练汇总 R/MFE/MAE/成本/逐 K 权益、策略版本对比和指标重算迁移 |
| 响应式 | 已有桌面和窄屏布局，并有长按补写、绘图手势锁、随机入口和移动表格等自动化验收 | 未完成真实手机设备与长历史性能验收、PWA、离线数据包和 Android 外壳 |

### 13.3 尚未实现

#### 数据与市场

- A 股和美股已有市场级手动增量流程；美股同步已按 Alpaca 交易日历、数据点预算和 URL 长度分批，并支持限速、暂停、恢复、失败拆分和重试。仍缺无人值守的收盘后定时调度，以及覆盖全历史的交易日历缺口扫描。
- 外汇首版数据闭环已经接入：服务端任务表、内置 Dukascopy 官方适配器、CSV 解析/聚合/入库、Twelve Data M1 增量、历史边界、失败恢复和数据源面板均已实现；回放已完成基于 BID 数据和冻结点差的 Bid/Ask 成交方向、合约/点值/币种换算及保证金强平。仍需补完整覆盖矩阵、缺口修复、边界对比报告、动态/真实 Ask 序列、交易周和隔夜费。
- XAUUSD/黄金样例、正式数据适配器、覆盖管理和成交规则尚未接入。
- 美股尚未形成覆盖全部可得历史的全市场深历史仓库；A 股分钟线与未复权/前复权/后复权多数据集也未形成全市场仓库。TdxQuant 当前只保存本机端点和显示连接状态，分钟批量读取连接器未接入。
- 下载任务已有暂停恢复和基础质量计数；v2 快照已改为按周期分桶、内容寻址分块和范围读取，并继续兼容旧差异链。仍缺快照引用可视化、未引用分块回收、创建阶段流式化、股票交易日历缺口/异常目录、授权备注、跨来源修补、备份恢复和存储空间管理。
- 未复权/前复权/后复权数据集的选择、锁定和训练报告提示。

#### Replay 与训练模式剩余项

- 指定日期/指定 K 线起点、训练长度自动结束、自由训练、盲测、测试区间、错题重练和隐藏品种/日期/绝对价格均已完成；仍缺回放控制条的“推进到下一交易日”快捷动作和已用训练时间。当前“次日开盘平仓”只属于交易委托，不等同于回放导航。
- 新建训练和随机训练已经拆为两个独立入口；随机训练支持普通/盲测选择及单局结束后的继续、复盘和退出闭环。侧栏训练设置可控制随机品种池、市场、周期、历史时间段、默认长度与是否纳入指数；A 股指数范围和美股流动性规则只在随机范围可能命中对应市场时显示，FX 不再出现无效限制。指数仅进入看盘训练，普通随机默认排除所有当前不可交易品种。后续随真实品种库扩充自选池、板块池和标签池。
- 错题重练目前按低于 80% 的正式计划与市场规则拒单生成；仍缺用户手动标错、错题标签、难度、间隔重复、多题队列和系统题库。
- 独立训练首页、训练日历、连续训练天数、最近问题提醒和策略版本表现；“继续上次训练”已经通过启动自动恢复和复盘历史入口实现。

#### 成交规则与风控

- A 股主板、科创板、创业板和北交所普通股票的基础 T+1、买入数量、10%/20%/30% 涨跌幅、IPO 初期无涨跌幅和默认禁止做空已完成；仍缺正式交易所日历/时段、停牌、ST/风险警示、除权除息、退市整理和零股卖出等特殊证券细则。
- 美股常规交易时段、做空开关及盘前盘后边界。
- 外汇已完成 BID/Ask 方向、手数、合约规格、Pip/Point 价值、币种换算、杠杆、保证金和强平；仍缺动态/真实 Ask 序列、隔夜费和交易周规则。黄金对应规则仍未接入。
- 初始资金、现金购买力、账户权益、佣金、最低佣金、通用点差/滑点、权益风险百分比仓位，以及外汇占用/可用保证金、保证金水平和逐 K 强平已完成；仍缺固定金额风险，以及按具体市场/券商自动选择费用模板。
- 止盈止损同根冲突已有保守、乐观和种子确定路径；部分成交、余量挂单、图表保护价选择和收紧式移动止损已完成。仍缺依赖更细粒度 K 线或 Tick 的真实路径回放，以及由用户配置的分批退出/多目标止盈。

#### 审计、复盘与策略优化

- A 股基础市场规则和独立成交引擎都已版本化并随训练冻结，完整 Session Event Log 已落库；仍缺由事件驱动的一键全量重算、旧引擎迁移和双版本差异报告。
- 确定性指标引擎已能把逐笔指标追溯到入场、出场、MFE 和 MAE 的 K 线时间戳；仍缺点击时间戳直接回到图表、跨训练证据索引和指标定义版本迁移。
- 策略模板、策略版本、规则差异、训练样本/验证样本分离。
- 候选规则、反例、副作用说明和下一轮验证任务。
- AI 总结、结论样本量门槛、案例链接及接受/拒绝/待验证状态。
- 错题本、专项训练、K 线回看库和相似场景聚合。

#### 导出、同步与移动端

- Markdown、JSON、交易 CSV、图表 PNG 和完整本地备份/恢复。
- 用户对象的 `version / deletedAt / deviceId / syncState` 等同步预留字段尚未全面落库。
- PWA、Android 外壳和选择性离线行情数据包。
- 账号与云同步按计划继续暂缓。

### 13.4 当前测试缺口

- 成交引擎已有独立领域测试，覆盖下一根开盘、市价/限价/止损触发、跳空、手续费/点差/滑点、保护单生效边界、同 K 线冲突、现金与拒单、部分开仓、余量延续及部分平仓；风险仓位覆盖成本预算、数量增量、现金上限和错误方向止损。仍缺组件层的长序列随机回放、移动止损的自动拖拽回归和跨引擎版本迁移测试。
- A 股主板、科创板、创业板和北交所的基础数量、T+1、涨跌幅、IPO 无涨跌幅及保守涨跌停成交已有领域测试；仍缺停牌/ST/除权等特殊证券规则，以及美股交易时段/做空规则测试。美股“最近完整收盘日”已有独立测试，但它只服务行情新鲜度，不等于成交时段测试。
- 指定日期/第 N 根起点、固定长度、随机时间窗、测试区间、盲测隐藏和自动结束已有领域测试；显式保存、训练卡盈亏和设置面板已完成浏览器验收。移动端已有关键静态与交互测试，仍缺真实设备、性能和超大历史错题队列测试。
- 缺少 5m→1h、日线→周线的交易所时区/日历及未来隔离测试。
- 确定性指标已有 R、MFE/MAE、后续路径、期望值、盈亏比、PF、逐 K 回撤、连亏与证据缺失的领域测试，并完成桌面/窄屏真实浏览器流程验收；仍缺点击证据跳转、跨训练新指标汇总、真实移动设备和长历史性能测试。
- v2 快照已有分桶边界、分块大小、哈希稳定性、内容复用、范围读取、左侧回看、全局索引和 v1 兼容测试；仍缺真实千万级 K 线压力测试、创建中断后的自动清理/续建、并发创建同一快照和未引用分块回收测试。
- 生产构建、ESLint 和项目测试均为绿色门禁；独立 `tsc --noEmit` 目前还不是绿色门禁，现有 Cloudflare 运行时类型、KLineChart 类型和 TypeScript 导入扩展名配置需要单独收敛。

### 13.5 建议接下来的实现顺序

1. **市场规则落地**：在通用成交引擎上补美股常规时段、做空开关和盘前盘后边界；补 A 股停牌/ST/除权等特殊证券；外汇 Bid/Ask、合约、点值、币种换算和保证金已完成，下一步补交易周、隔夜费与动态/真实 Ask 序列。
2. **复盘闭环补强**：把单场 R/MFE/MAE/成本/逐 K 回撤汇总到整体表现；增加证据时间戳点击回到图表、指标定义说明和旧记录重算/迁移。
3. **仓位与执行深化**：在已完成权益风险百分比、部分成交、移动止损和外汇保证金强平的基础上，补固定金额风险、用户指定分批退出/多目标止盈；增加事件驱动的独立状态重放器。
4. **数据完整性**：补交易日历缺口扫描、异常隔离、未完成高周期的未来隔离和无人值守收盘后调度；继续完成 TdxQuant 分钟连接器和复权数据集管理。
5. **外汇与黄金**：外汇数据任务主链已完成，下一步补覆盖矩阵、缺口修复、边界对比和专用成交成本；之后接入 XAUUSD/黄金正式适配器。
6. **训练闭环**：补结构化 `no-trade` 与决策字段、策略版本、错题队列和专项训练，再接带样本量、反例和验证任务的 AI 总结。

### 13.6 未完成功能详细清单与验收口径

下面只列代码中尚未形成完整闭环的能力；“入口已出现”不等于“功能已完成”。优先级按当前可用 MVP 的正确性和用户价值排序。

| ID / 优先级 | 功能 | 当前真实边界 | 完成验收口径 |
|---|---|---|---|
| `MKT-01` / P0 | 市场专用成交规则 | A 股普通股票已有分板块数量、T+1、涨跌幅和 IPO 初期规则；FX 已使用独立 `fx-spot-margin` 规则完成 Bid/Ask 方向、点值、合约、币种换算和保证金强平；美股和黄金仍不完整 | A 股补正式交易日历/时段、停牌、ST、除权、退市整理和零股卖出；美股补常规时段、盘前盘后与可配置做空；FX 补交易周、隔夜费和动态/真实 Ask；黄金补独立合约；规则版本随训练冻结并有边界测试 |
| `REV-01` / P0 | 跨训练确定性复盘 | R、MFE/MAE、成本、路径和逐 K 回撤已在单场快照/明细中；整体表现仍主要使用旧的盈亏、胜率、PF 和训练序列回撤 | 表现页可按现有筛选条件汇总净 R、平均 R、MFE/MAE、费用侵蚀、逐 K 权益回撤和证据完整率；口径显示版本，结果可追到具体训练和交易 |
| `REV-02` / P0 | 证据跳转与指标迁移 | 已保存入场、出场、MFE、MAE 和回撤时间戳，但它们只是文本证据 | 点击证据可打开绑定快照并定位对应 K 线；旧指标能按定义版本批量重算，失败记录明确列出原因，原结果可保留对比 |
| `AUD-01` / P0 | 事件驱动状态重放 | 有有序事件日志，也保存完整状态 JSON；当前恢复依赖状态快照，不是从事件重建 | 独立 Replayer 可从初始状态逐事件重建游标、订单、仓位、成交、决策和绘图；重建结果与保存状态做哈希比对；支持旧/新引擎双跑差异报告 |
| `DAT-01` / P0 | 数据质量闭环 | A 股近 30 日回查、FX 基础质量摘要和下载重试已完成；没有统一的全历史日历缺口与异常工作台 | 按市场日历扫描缺口、重复、非法 OHLC、异常跳空和来源边界；异常进入隔离队列，可重试/换源修复；任何修复生成新数据版本且不改写已引用快照 |
| `DAT-02` / P0 | 高周期未来隔离 | 已保存的 1h/1d/1w 可回放；低周期切换高周期时还没有动态未完成 K 线 | 从已揭示低周期即时聚合当前未完成高周期，结果不得读取未来低周期；覆盖跨日、跨周、时区、节假日和夏令时测试 |
| `DAT-03` / P1 | 快照与长历史工程化 | v2 已分块、去重、按范围读取，恢复训练和表现上下文已使用窗口；但新建快照仍先加载完整序列，初次创建响应也返回全量 K 线 | 快照构建和首次训练加载均可流式/分页；创建可续建或安全回滚；有引用图、空间统计和未引用分块回收；千万级 K 线下内存与响应时间达到明确预算 |
| `EXE-01` / P1 | 风险仓位与高级执行剩余项 | 已有初始资金、现金预留、多独立仓位、固定数量、权益百分比风险反算、成本预算、部分成交/余量委托、部分关闭切片和收紧式移动止损；FX 已完成合约乘数、点值、保证金、可用保证金和逐 K 强平；UI 目前仍以整仓平仓和单一目标为主 | 增加固定金额风险、用户指定减仓数量/比例、多级止盈与分批规则；为后续其他杠杆市场复用保证金规格，并保证所有快进仍逐根执行 |
| `TRN-01` / P1 | 结构化决策与策略版本 | 决策卡已有市场状态、位置、理由、止损、目标和说明，提交后冻结；方向、触发和放弃条件仍依赖文本或交易动作 | 增加主导方、方向、触发价、放弃条件、置信度、`no-trade`、分批/移动逻辑和策略版本；可比较同一策略版本在训练集/验证集的表现 |
| `TRN-02` / P1 | 错题本与训练首页 | 已能按低分计划/规则拒单生成单次错题重练；没有可维护题库和训练节奏 | 用户可手动标错、加标签/难度，形成多题队列；支持间隔重复、专项训练、完成记录、训练日历、连续天数和最近问题提醒 |
| `DAT-04` / P1 | 数据源覆盖扩展 | A 股 TDX/Tushare 日线、美股 Alpaca SIP 日线和六个 FX 品种已接入；TdxQuant 只有端点设置，黄金只有管理页签 | 接通 TdxQuant 分钟批量读取；建立 A 股复权数据集选择与锁定；FX 增加覆盖矩阵/边界报告；XAUUSD 完成目录、下载、覆盖、训练和成交规则的端到端闭环 |
| `OPS-01` / P1 | 调度、导出与备份 | 市场更新只能在页面手动触发；没有用户数据导出和整机迁移流程 | 支持按交易所收盘时间无人值守更新、失败通知与补跑；可导出 Markdown、JSON、交易 CSV、图表 PNG；用户数据可完整备份/校验/恢复，行情缓存与用户资产分开处理 |
| `MOB-01` / P2 | PWA、离线与 Android | 有响应式和窄屏自动验收，但没有安装清单、Service Worker、离线包或原生外壳 | 真机完成触控和长历史性能验收；PWA 可安装并选择性缓存行情；断网可完成已下载训练；Android 外壳能安全访问本地数据并迁移用户备份 |
| `AI-01` / P2 | AI 策略改进 | 只有确定性事实和习惯统计，没有模型调用、候选规则状态或样本量门槛 | AI 只能引用已计算事实并链接案例；输出候选规则、反例、副作用和下一轮验证任务；每条建议可接受/拒绝/待验证，低样本明确降级而不是下结论 |

另外两项属于明确的产品边界，不应误记成“已经完成”：顶部“实盘筛选”目前是最近完整收盘日扫描加本地观察账本，不连接券商下单；账号和云同步仍按计划暂缓，只有部分本机对象结构为以后同步预留。

## 14. 参考资料

- [FastBull Charts / Replay](https://www.fastbull.com/traders/chart)
- [深交所：主板股票交易机制投资者问答](https://investor.szse.cn/knowledge/qa/t20230306_599093.html)
- [上交所：上海证券交易所交易规则（2026年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/trade/universal/c/c_20260424_10816492.shtml)
- [KLineChart：Overlay 文档](https://klinecharts.com/en-US/guide/overlay.html)
- [KLineChart：Replay/本地数组适用的数据接入方式](https://klinecharts.com/en-US/guide/data-integration)
- [KLineChart GitHub 与 Apache 2.0 License](https://github.com/klinecharts/KLineChart)
- [TradingView Lightweight Charts](https://www.tradingview.com/lightweight-charts/)
- [Tushare Pro：A 股数据与接口目录](https://tushare.pro/document/2?doc_id=17)
- [Tushare Pro：A 股日线行情](https://tushare.pro/document/1?doc_id=27)
- [Tushare Pro：历史分钟行情](https://tushare.pro/document/2?doc_id=370)
- [Alpaca：Historical Stock Bars](https://docs.alpaca.markets/reference/stockbars)
- [Alpaca：Market Data Plans](https://docs.alpaca.markets/docs/about-market-data-api)
- [Dukascopy：Forex Historical Data Export](https://www.dukascopy.com/swiss/english/marketwatch/historical/)
- [Dukascopy Historical Data Service](https://www.dukascopy.com/wiki/en/development/strategy-api/historical-data/historical-data-service/)
- [Twelve Data：Forex API v2](https://support.twelvedata.com/en/articles/12520817-forex-api-v2)
- [Twelve Data：API Documentation](https://twelvedata.com/docs)
- [Investor.gov：Foreign Currency Exchange / OTC Forex](https://www.investor.gov/introduction-investing/general-resources/news-alerts/alerts-bulletins/foreign)

---

### 修改记录

- `v0.31`：成交引擎升级为 `2026.08-v2`，增加按单根成交量参与率的部分开仓/平仓、余量挂单和现金预留按比例释放；增加权益风险百分比仓位并把点差、滑点和双边佣金计入预算。止损/止盈改为图表点击取价并与事前决策卡联动，取价时立即新增或更新自动保护计划，正式提交时替换自动卡；持仓保护线支持拖动收紧。随机训练的 A 股指数与美股流动性限制按市场范围条件显示。生产构建、ESLint、130 项测试和本地浏览器交互验收通过。
- `v0.32`：成交引擎升级为 `2026.08-v4`。外汇以 BID K 线为图表基准，使用冻结点差生成 Ask；买入/回补、卖出/平多、条件单和保护单均按正确报价侧执行。数量改为 0.01 手增量并冻结 10 万基础币合约、Pip/Point、账户币种和换算率；USD 账户覆盖六个首发货币对的直接/反向换算。资金账户增加杠杆、占用/可用保证金、保证金水平、拒单和逐 K 强平，强平会取消余单；相关字段随训练、成交与复盘证据保存。
- `v0.30`：按 2026-08-11 代码重新核对文档；补记 v2 内容寻址分块快照、按训练区间/左侧历史窗口读取、全局索引恢复、旧 v1 快照兼容和相应自动测试；删除“新快照仍使用差异链”的过时描述，并新增按 P0/P1/P2 排序、带当前边界与验收口径的未完成功能清单。
- `v0.29`：抽离 `2026.08-v1` 纯函数成交引擎，落地市价/限价/止损触发单、跳空、自动止盈止损、佣金/点差/滑点、三种同根冲突策略、市场规则拦截与逐根快速推进；保存训练锁定引擎和参数。新增确定性单场复盘指标：净/毛盈亏、费用、R、MFE/MAE、出场效率、后续路径、期望值、盈亏比、PF、逐 K 回撤/水下时长、连亏及证据时间戳；生产构建、ESLint 与 124 项测试通过，并完成桌面/手机宽度的真实页面验收。
- `v0.28`：按 2026-08-08 代码与 104 项自动测试重新核对实现状态；补记 1m/FX、资金账户、A 股多板块基础规则、绘图对象树、MA/EMA 和实盘观察账本，移除已完成事项；将成交引擎、确定性复盘、市场规则、数据完整性、策略版本和 AI 总结重新列为下一阶段主线，并明确 Alpaca 历史数据当前采用 SIP-only 语义。
- `v0.20`：明确外汇数据闭环采用 Dukascopy 官方 CSV 历史基准 + Twelve Data REST 增量；新增网页自动下载、服务端流式处理、M1 分块导入、5m 基准聚合、历史切换点、跨来源校验、失败恢复、来源追溯、点差与数据授权要求，并记录当前 CSV 导入器需要改造成后台分块任务。
- `v0.21`：落地 FX 首版任务闭环：新增六个首批货币对目录、外汇任务表与 API、Dukascopy CSV 服务端解析/聚合、Twelve Data 增量、服务端凭证设置和数据源维护面板；Dukascopy 动态导出页改为由内置适配器读取官方配置并解码，外部 CSV 地址仅作为可选覆盖。
- `v0.22`：新增内置 Dukascopy 官方适配器：读取 widget 配置、解码 Jetta 分钟数据、处理品种可用起点、提供同源 CSV API；Dukascopy 设置改为无需填写地址，保留自定义端点覆盖能力。
- `v0.23`：修正 Jetta 官方品种代码与 BID 分钟路径，增加 widget 测试主机不可达时的官方生产主机回退、请求超时和可读错误诊断；验证 2000 年历史任务会自动从官方实际分钟数据起点续跑。
- `v0.24`：历史任务保留 Dukascopy 原始 M1 到数据库，同时继续生成 5m、1h、1d、1w；训练周期筛选、随机周期、复盘筛选和 K 线图增加 1m。已有仅保存高周期的数据不能反推 M1，需要重新下载对应历史范围。
- `v0.25`：Dukascopy 内置适配器改为优先使用正式 Jetta 主机；单日请求对网络错误、超时、429 和 5xx 自动重试并指数退避，30 日分片内采用最多 3 路受控并发，降低频繁 `Network connection lost` 导致的整段失败并缩短历史下载时间；文档明确当前 Twelve Data 连接器仍只写入 5m，供应商 API 本身支持 1min。
- `v0.26`：Twelve Data 增量由 5min 改为 1min：只拉取和持久化已收盘 M1，再由本地生成 5m/1h/1d/1w；游标推进单位改为 1 分钟，Dukascopy 与 Twelve Data 分别维护历史边界和增量检查点，并忽略旧 5m 分页游标以防止 M1 缺口。
- `v0.27`：修复长时间 FX 历史任务显示“等待开始”和刷新超时：历史任务改为 7 天小分片、4 天并发并保留断点，跨分片复用品种元数据且不再回退不可达测试节点；M1 使用 JSON 分批写库和有界覆盖统计，已有高周期覆盖时只补 M1；高周期聚合查询增加当前分片结束边界，避免补早期 M1 时把未来全部 5m 载入内存；界面增加分片写入心跳、分片用时与最近活动时间，重复运行锁防止刷新后并发执行同一任务。
- `v0.19`：A 股 TDX 初始化后新增 Tushare 120 市场级维护流程；按日期拉取全市场普通股票不复权日线，实现从最后日期增量、最近 30 日缺口与修正回查、限速、暂停/恢复、Token 不落任务文件，以及 SQLite 变化覆盖层与日/周线合并读取；启动 BAT 增加数据服务就绪等待与真实端口占用识别。
- `v0.18`：数据库页新增 A股、美股、外汇、黄金市场切换，各市场独立统计、任务和覆盖明细；移除 Tushare/Alpaca 单标的代码输入，A股改为初始化后的最新全市场包更新，美股改为 Alpaca 活跃证券目录批量初始化与按最后日期增量更新，并以串行限速降低本机资源占用。
- `v0.17`：通达信完整包导入后自动同步在线中文证券目录，支持手动刷新名称；数据库覆盖表新增逐行选择、当前页全选与二次确认删除，样例删除后不再被自动重新写入，不可变训练快照继续保留。
- `v0.16`：接入通达信官方沪深京日线完整包的本机服务；支持 HTTP Range 断点续传、ZIP 与 SHA-256 校验、可恢复任务、证券分类、历史范围裁剪、原生 `.day` 分区存储、日线/周线统一读取、覆盖统计与不可变快照回退；BAT 同时启动网页与数据服务。
- `v0.15`：数据页新增快速初始化与四步高级自定义向导；高级方案支持通达信 ZIP、Tushare、TdxQuant、Alpaca 和本地文件的来源编排，TdxQuant 本机端点归入设置；不可变行情快照升级为完整检查点＋差异链存储。
- `v0.14`：移除图表初始化阶段对全局 `ResizeObserver` 的替换；仅拦截浏览器已知的两条 ResizeObserver 循环警告，避免本地开发错误浮层遮挡页面，同时保留其他真实脚本错误提示。
- `v0.13`：将 Alpaca/Tushare 个人凭证集中到侧栏“设置 → 数据源设置”；支持应用内保存、替换和清除，数据库页只保留行情下载与任务管理，环境文件降为兼容备用。
- `v0.12`：接入 Tushare A 股与 Alpaca 美股历史 K 线适配器；新增本地凭证状态、5m/1h/1d/1w 下载表单、持久化分页任务、暂停/恢复/重试、基础质量报告和动态 Replay 品种目录。
- `v0.11`：新增独立训练表现板块；汇总全部训练并支持按品种、周期、模式、状态和日期筛选训练集，展示组合盈亏、胜率、Profit Factor、最大回撤、计划完整度及胜负分布；训练明细可进入复盘或继续训练。
- `v0.10`：随机训练从新建训练中拆出为独立按钮；支持普通随机/盲测随机选择，并在单局结束后提供继续随机、查看复盘和 X 退出；延迟图表尺寸监听回调，避免开发环境的 `ResizeObserver` 循环报错遮罩。
- `v0.9`：训练改为仅手动或结束保存；可恢复训练卡新增模式、状态、区间、进度和保存时盈亏；统一新建入口，启用侧栏基本/训练设置并支持随机品种、市场、周期与时间段；补充盲测解释。
- `v0.8`：新增版本化 Replay 训练任务；实现日期/第 N 根/随机起点、训练长度自动结束、自由训练、盲测、测试区间、错题重练，以及品种、日期、绝对价格隐藏和盲测答案锁定。
- `v0.7`：新增版本化 A 股主板普通股票规则；实现禁做空、买入 100 股整手、T+1、10% 涨跌幅、涨跌停保守成交、拒单记录，并将完整规则快照绑定训练、订单、事件日志和复盘。
- `v0.6`：将决策草稿与正式提交分离；新增不可变决策记录、K线“计划”标记、完整决策复盘、历史训练直接查看和可展开操作时间线。
- `v0.5`：新增 SHA-256 内容寻址的不可变行情快照、独立有序 Session Event Log、跨行情数据版本恢复，以及可恢复训练的确认删除功能。
- `v0.4`：移除 Sites 托管配置，明确纯本地运行；补充 2026-07-23 当前实现、部分实现、未实现和测试缺口盘点。
- `v0.3`：将数据范围收紧为详细 K 线库；暂缓财务、新闻、公司行动明细、Tick/盘口等其他数据，只保留读取、聚合和校验 K 线所需的最小目录。
- `v0.2`：确认 A 股/美股优先，外汇/黄金紧随；核心周期调整为 5m/1h/1d/1w；补充详尽数据库及管理中心、动态高周期聚合、云同步预留和 Android 路线。
- `v0.1`：建立产品定位、FastBull 对标、多市场数据方案、Replay/模拟交易规则、标记体系、复盘与策略版本框架。
